<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AutoSpect AI — Vehicle Inspection Platform</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

<!-- Firebase SDK -->
<script type="module">
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
  import { getAnalytics } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-analytics.js";
  import { getAuth, createUserWithEmailAndPassword, signInWithEmailAndPassword, signOut, onAuthStateChanged }
    from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
  const firebaseConfig = {
    apiKey: "AIzaSyDqSqaabGEg-0AF8_JPh1gJ5tMKE1D0iIo",
    authDomain: "autospect-ai.firebaseapp.com",
    projectId: "autospect-ai",
    storageBucket: "autospect-ai.firebasestorage.app",
    messagingSenderId: "779394892034",
    appId: "1:779394892034:web:469dd4b5c61e7ff844821b",
    measurementId: "G-5S6PF7DMMZ"
  };
  const app = initializeApp(firebaseConfig);
  const analytics = getAnalytics(app);
  const auth = getAuth(app);
  window._fbAuth = auth;
  window._fbCreateUser = createUserWithEmailAndPassword;
  window._fbSignIn = signInWithEmailAndPassword;
  window._fbSignOut = signOut;
  window._fbOnAuth = onAuthStateChanged;
  onAuthStateChanged(auth, user => {
    window._currentUser = user || null;
    if (window._handleAuthChange) window._handleAuthChange(user);
  });
</script>

<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --c:#c8d8e4;--cb:rgba(200,216,228,0.15);--cg:rgba(200,216,228,0.5);
  --b:#000;--s:#0a0a0a;--p:#111;--m:#1a1a1a;
  --t:#fafafa;--tm:#9ca3af;--td:#4b5563;
  --red:#ff4444;--green:#4ade80;--yellow:#f59e0b;
  --r:14px;--r2:20px;
}
html{scroll-behavior:smooth}
body{font-family:'Syne',sans-serif;background:var(--b);color:var(--t);overflow-x:hidden;line-height:1.5}
.bg{position:fixed;inset:0;pointer-events:none;z-index:0}
.bg-grid{position:absolute;inset:0;opacity:.05;background-image:linear-gradient(rgba(200,216,228,.4)1px,transparent 1px),linear-gradient(90deg,rgba(200,216,228,.4)1px,transparent 1px);background-size:60px 60px}
.bg-orb{position:absolute;border-radius:50%;filter:blur(130px);animation:orbf 20s ease-in-out infinite}
.bg-orb.a{top:-300px;right:-200px;width:700px;height:700px;background:rgba(180,200,215,.1)}
.bg-orb.b{bottom:-200px;left:-300px;width:600px;height:600px;background:rgba(120,150,170,.08);animation-delay:-10s}
@keyframes orbf{0%,100%{transform:translate(0,0)}50%{transform:translate(60px,-60px)}}
nav{position:fixed;top:0;left:0;right:0;z-index:100;background:rgba(0,0,0,.7);backdrop-filter:blur(20px);border-bottom:1px solid rgba(200,216,228,.15)}
.nav-inner{max-width:1300px;margin:0 auto;padding:14px 24px;display:flex;align-items:center;justify-content:space-between}
.logo{display:flex;align-items:center;gap:10px;text-decoration:none;cursor:pointer}
.logo-name{font-size:1.1rem;font-weight:800;color:var(--c);letter-spacing:-.02em}
.nav-pills{display:flex;align-items:center;gap:10px}
.nav-pill{padding:8px 18px;border-radius:8px;font-weight:600;font-size:.875rem;cursor:pointer;border:none;font-family:'Syne',sans-serif;transition:all .25s;text-decoration:none;display:inline-block}
.nav-pill.ghost{background:transparent;color:var(--tm);border:1px solid rgba(255,255,255,.1)}
.nav-pill.ghost:hover{color:var(--t);border-color:rgba(200,216,228,.3)}
.nav-pill.cta{background:linear-gradient(135deg,#c8d8e4,#8aabb8);color:#0a0f12;font-weight:700}
.nav-pill.cta:hover{transform:translateY(-1px);box-shadow:0 6px 20px rgba(200,216,228,.3)}
#navUserInfo{font-size:.8rem;color:var(--tm);font-family:'JetBrains Mono',monospace;padding:6px 14px;background:rgba(200,216,228,.06);border:1px solid rgba(200,216,228,.15);border-radius:8px;display:none;align-items:center;gap:8px}
.page{display:none;position:relative;z-index:1;animation:pfade .4s ease}
.page.active{display:block}
@keyframes pfade{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:translateY(0)}}
.hero{min-height:100vh;display:flex;align-items:center;padding:100px 24px 60px}
.hero-inner{max-width:1300px;margin:0 auto;display:grid;grid-template-columns:1fr 1fr;gap:64px;align-items:center}
.hero-label{display:inline-flex;align-items:center;gap:8px;padding:6px 16px;background:rgba(200,216,228,.08);border:1px solid rgba(200,216,228,.25);border-radius:99px;font-size:.8rem;font-weight:600;color:var(--c);letter-spacing:.06em;margin-bottom:24px;font-family:'JetBrains Mono',monospace}
.hero h1{font-size:clamp(2.8rem,5vw,4.2rem);font-weight:800;line-height:1.05;margin-bottom:20px;letter-spacing:-.03em}
.hero h1 .line1{background:linear-gradient(90deg,#fff,rgba(255,255,255,.7));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero h1 .line2{background:linear-gradient(90deg,var(--c),#a0b8c8);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero-sub{font-size:1.05rem;color:var(--tm);max-width:480px;margin-bottom:36px;line-height:1.65}
.hero-btns{display:flex;gap:12px;flex-wrap:wrap}
.btn{display:inline-flex;align-items:center;gap:8px;padding:12px 28px;border-radius:10px;font-weight:700;font-size:.95rem;cursor:pointer;border:none;font-family:'Syne',sans-serif;transition:all .25s;text-decoration:none}
.btn-primary{background:linear-gradient(135deg,#c8d8e4,#8aabb8);color:#0a0f12;box-shadow:0 0 20px rgba(200,216,228,.2)}
.btn-primary:hover{transform:translateY(-2px);box-shadow:0 8px 28px rgba(200,216,228,.35)}
.btn-ghost{background:transparent;color:var(--t);border:1px solid rgba(255,255,255,.15)}
.btn-ghost:hover{border-color:rgba(200,216,228,.4);color:var(--c)}
.btn-outline{background:transparent;color:var(--c);border:1px solid rgba(200,216,228,.4)}
.btn-outline:hover{background:rgba(200,216,228,.08);transform:translateY(-1px)}
.btn-success{background:linear-gradient(135deg,#4ade80,#22c55e);color:#000;font-weight:700}
.btn-success:hover{transform:translateY(-2px);box-shadow:0 8px 24px rgba(74,222,128,.3)}
.btn-danger-live{background:rgba(255,68,68,.15);color:var(--red);border:1px solid rgba(255,68,68,.3);display:inline-flex;align-items:center;gap:8px;padding:12px 28px;border-radius:10px;font-weight:700;font-size:.95rem;cursor:pointer;font-family:'Syne',sans-serif;transition:all .25s}
.btn-danger-live:hover{background:rgba(255,68,68,.25)}
.hero-img{position:relative;overflow:hidden}
.hero-img img{width:100%;display:block}
.scan-bar{position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#e8f0f5,rgba(255,255,255,.9),#e8f0f5,transparent);animation:scan 3s linear infinite;filter:blur(1px)}
@keyframes scan{0%{top:-2px;opacity:0}10%{opacity:1}90%{opacity:1}100%{top:100%;opacity:0}}
.stats-row{max-width:1300px;margin:0 auto 80px;display:grid;grid-template-columns:repeat(4,1fr);gap:20px}
.scard{background:rgba(255,255,255,.03);border:1px solid rgba(200,216,228,.12);border-radius:16px;padding:28px 20px;text-align:center;transition:all .3s}
.scard:hover{border-color:rgba(200,216,228,.3);transform:translateY(-4px)}
.scard-val{font-size:2.4rem;font-weight:800;background:linear-gradient(90deg,var(--c),#a0b8c8);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin:10px 0 6px}
.scard-lbl{font-size:.75rem;color:var(--tm);text-transform:uppercase;letter-spacing:.1em;font-family:'JetBrains Mono',monospace}
.section{max-width:1300px;margin:0 auto;padding:0 24px 80px}
.section-hd{text-align:center;margin-bottom:52px}
.section-hd h2{font-size:clamp(2rem,4vw,3rem);font-weight:800;letter-spacing:-.03em;background:linear-gradient(90deg,#fff,var(--c));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:12px}
.section-hd p{color:var(--tm);font-size:1.05rem;max-width:560px;margin:0 auto}
.feat-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px}
.fcard{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:18px;padding:32px 28px;transition:all .35s;cursor:default}
.fcard:hover{border-color:rgba(200,216,228,.35);transform:translateY(-6px);background:rgba(200,216,228,.04)}
.fcard-ico{width:50px;height:50px;background:rgba(200,216,228,.08);border:1px solid rgba(200,216,228,.25);border-radius:12px;display:flex;align-items:center;justify-content:center;margin-bottom:20px;transition:all .3s}
.fcard:hover .fcard-ico{background:rgba(200,216,228,.18);transform:scale(1.1)}
.fcard h3{font-size:1.15rem;font-weight:700;margin-bottom:10px}
.fcard p{color:var(--tm);font-size:.9rem;line-height:1.6}
.steps-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px}
.step{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:18px;padding:32px 28px;transition:all .3s}
.step:hover{border-color:rgba(200,216,228,.3);transform:translateY(-4px)}
.step-num{font-size:4rem;font-weight:800;color:rgba(200,216,228,.12);line-height:1;margin-bottom:16px;font-family:'JetBrains Mono',monospace}
.step h3{font-size:1.15rem;font-weight:700;margin-bottom:10px}
.step p{color:var(--tm);font-size:.9rem;line-height:1.6}
.cta-wrap{max-width:1300px;margin:0 auto;padding:0 24px 80px}
.cta-box{background:linear-gradient(135deg,rgba(200,216,228,.12),rgba(138,171,184,.12));border:1px solid rgba(200,216,228,.25);border-radius:24px;padding:64px 48px;text-align:center}
.cta-box h2{font-size:clamp(1.8rem,3.5vw,2.8rem);font-weight:800;background:linear-gradient(90deg,#fff,var(--c));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:16px}
.cta-box p{color:#d1d5db;font-size:1.05rem;max-width:600px;margin:0 auto 32px}
footer{position:relative;z-index:1;border-top:1px solid rgba(200,216,228,.12);padding:48px 24px 32px}
.foot-inner{max-width:1300px;margin:0 auto;display:grid;grid-template-columns:repeat(4,1fr);gap:40px;margin-bottom:32px}
.foot-brand span{font-size:1rem;font-weight:800;color:var(--c);display:block;margin-bottom:8px}
.foot-brand p{font-size:.85rem;color:var(--tm)}
.foot-col h5{font-size:.85rem;font-weight:700;color:#fff;margin-bottom:14px;text-transform:uppercase;letter-spacing:.08em}
.foot-col a{display:block;color:var(--tm);text-decoration:none;font-size:.85rem;margin-bottom:8px;transition:color .2s;cursor:pointer}
.foot-col a:hover{color:var(--c)}
.foot-bottom{max-width:1300px;margin:0 auto;padding-top:24px;border-top:1px solid rgba(255,255,255,.06);text-align:center;font-size:.8rem;color:var(--td);font-family:'JetBrains Mono',monospace}
#dashboard{padding-top:80px;min-height:100vh}
.dash-wrap{max-width:1300px;margin:0 auto;padding:40px 24px}
.dash-hd{margin-bottom:40px}
.dash-hd h1{font-size:2.4rem;font-weight:800;letter-spacing:-.03em;background:linear-gradient(90deg,#fff,var(--c));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:8px}
.dash-hd p{color:var(--tm);font-size:1rem}
.prog-steps{display:flex;align-items:center;gap:0;margin-bottom:44px;background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:14px;overflow:hidden}
.pstep{flex:1;display:flex;align-items:center;justify-content:center;gap:10px;padding:16px 12px;font-size:.85rem;font-weight:600;color:var(--tm);cursor:pointer;transition:all .3s;border-right:1px solid rgba(200,216,228,.08)}
.pstep:last-child{border-right:none}
.pstep.done{color:#4ade80}
.pstep.active{color:var(--c);background:rgba(200,216,228,.07)}
.pstep .ps-num{width:24px;height:24px;border-radius:50%;border:1.5px solid currentColor;display:flex;align-items:center;justify-content:center;font-size:.75rem;font-family:'JetBrains Mono',monospace;flex-shrink:0}
.pstep.done .ps-num{background:#4ade80;border-color:#4ade80;color:#000}
.pstep.active .ps-num{background:var(--c);border-color:var(--c);color:#000}
.panel{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:18px;padding:28px;margin-bottom:24px}
.panel-title{font-size:1rem;font-weight:700;margin-bottom:20px;display:flex;align-items:center;gap:10px;color:var(--t)}
.panel-title svg{color:var(--c)}
.form-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
.form-group{display:flex;flex-direction:column;gap:6px}
.form-group label{font-size:.8rem;font-weight:600;color:var(--tm);text-transform:uppercase;letter-spacing:.06em;font-family:'JetBrains Mono',monospace}
.form-group input{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:10px 14px;color:var(--t);font-family:'Syne',sans-serif;font-size:.9rem;transition:border-color .2s;outline:none}
.form-group input:focus{border-color:rgba(200,216,228,.5);background:rgba(200,216,228,.04)}
.form-group input::placeholder{color:var(--td)}
.upload-zone{border:2px dashed rgba(200,216,228,.2);border-radius:14px;padding:44px 24px;text-align:center;cursor:pointer;transition:all .3s;background:rgba(200,216,228,.02)}
.upload-zone:hover,.upload-zone.drag{border-color:rgba(200,216,228,.5);background:rgba(200,216,228,.05)}
.upload-zone .uz-ico{color:var(--c);margin-bottom:14px}
.upload-zone p{color:var(--tm);font-size:.9rem;margin-bottom:4px}
.upload-zone span{font-size:.8rem;color:var(--td)}
#fileInput,#audioInput{display:none}
.img-preview-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:16px;margin-top:16px}
.img-thumb{position:relative;aspect-ratio:16/10;border-radius:12px;overflow:hidden;border:1px solid rgba(200,216,228,.2);transition:all .25s}
.img-thumb:hover{border-color:rgba(200,216,228,.45);transform:translateY(-3px)}
.img-thumb img{width:100%;height:100%;object-fit:cover}
.img-thumb-rm{position:absolute;top:8px;right:8px;width:26px;height:26px;background:rgba(0,0,0,.75);border-radius:50%;display:flex;align-items:center;justify-content:center;cursor:pointer;color:#fff;font-size:13px;border:none;transition:background .2s;backdrop-filter:blur(4px)}
.img-thumb-rm:hover{background:var(--red)}
.mode-select-grid{display:grid;gap:16px;margin-bottom:28px}
.mode-select-card{background:rgba(255,255,255,.025);border:2px solid rgba(200,216,228,.12);border-radius:14px;padding:24px 20px;text-align:center;cursor:pointer;transition:all .3s;font-family:'Syne',sans-serif}
.mode-select-card:hover{border-color:rgba(200,216,228,.4);background:rgba(200,216,228,.05);transform:translateY(-3px)}
.mode-select-card.active{border-color:var(--c);background:rgba(200,216,228,.08)}
.mode-select-card .msc-icon{font-size:2rem;margin-bottom:10px}
.mode-select-card h4{font-size:.95rem;font-weight:700;color:var(--t);margin-bottom:4px}
.mode-select-card p{font-size:.8rem;color:var(--tm)}
.btn:disabled{opacity:.5;cursor:not-allowed;transform:none!important}
.spinner{width:18px;height:18px;border:2px solid rgba(0,0,0,.3);border-top-color:#000;border-radius:50%;animation:spin .7s linear infinite}
.spinner-light{border-color:rgba(200,216,228,.2);border-top-color:var(--c)}
@keyframes spin{to{transform:rotate(360deg)}}
#scanOverlay{position:fixed;inset:0;z-index:9000;background:rgba(0,0,0,.92);backdrop-filter:blur(12px);display:none;flex-direction:column;align-items:center;justify-content:center}
#scanOverlay.active{display:flex}
.scan-ol-logo{font-size:1.1rem;font-weight:800;color:var(--c);letter-spacing:-.02em;margin-bottom:32px;font-family:'Syne',sans-serif;opacity:.7}
.scan-img-strip{display:flex;gap:12px;margin-bottom:36px}
.scan-img-slot{width:120px;height:80px;border-radius:10px;overflow:hidden;border:1px solid rgba(200,216,228,.15);position:relative;background:#0d0d0d;flex-shrink:0}
.scan-img-slot img{width:100%;height:100%;object-fit:cover;opacity:.55}
.scan-img-slot .scan-line{position:absolute;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#c8d8e4,rgba(255,255,255,.95),#c8d8e4,transparent);filter:blur(1.5px);animation:scanline 1.8s linear infinite;opacity:0}
.scan-img-slot.active-scan .scan-line{opacity:1}
.scan-img-slot .scan-corner{position:absolute;width:14px;height:14px;border-color:var(--c);border-style:solid;border-width:0}
.scan-img-slot .scan-corner.tl{top:4px;left:4px;border-top-width:2px;border-left-width:2px}
.scan-img-slot .scan-corner.tr{top:4px;right:4px;border-top-width:2px;border-right-width:2px}
.scan-img-slot .scan-corner.bl{bottom:4px;left:4px;border-bottom-width:2px;border-left-width:2px}
.scan-img-slot .scan-corner.br{bottom:4px;right:4px;border-bottom-width:2px;border-right-width:2px}
.scan-img-slot.done-scan{border-color:rgba(74,222,128,.4)}
.scan-img-slot .done-tick{position:absolute;inset:0;display:none;align-items:center;justify-content:center;background:rgba(0,0,0,.45);font-size:1.4rem}
.scan-img-slot.done-scan .done-tick{display:flex}
@keyframes scanline{0%{top:-4px;opacity:0}8%{opacity:1}92%{opacity:1}100%{top:100%;opacity:0}}
.scan-status-box{text-align:center;margin-bottom:28px}
.scan-status-label{font-family:'JetBrains Mono',monospace;font-size:.75rem;text-transform:uppercase;letter-spacing:.12em;color:var(--tm);margin-bottom:10px}
.scan-status-msg{font-size:1.1rem;font-weight:700;color:var(--t);min-height:1.5em}
.scan-progress-wrap{width:340px;max-width:80vw;margin-bottom:10px}
.scan-progress-track{height:3px;background:rgba(200,216,228,.1);border-radius:99px;overflow:hidden;margin-bottom:8px}
.scan-progress-fill{height:100%;background:linear-gradient(90deg,#8aabb8,#c8d8e4);border-radius:99px;width:0%;transition:width .5s ease}
.scan-progress-pct{font-family:'JetBrains Mono',monospace;font-size:.75rem;color:var(--tm);text-align:center}
.scan-dots{display:flex;gap:6px;justify-content:center;margin-top:20px}
.scan-dot{width:6px;height:6px;border-radius:50%;background:rgba(200,216,228,.25);animation:dotpulse 1.4s ease-in-out infinite}
.scan-dot:nth-child(2){animation-delay:.2s}.scan-dot:nth-child(3){animation-delay:.4s}
@keyframes dotpulse{0%,80%,100%{transform:scale(1);background:rgba(200,216,228,.25)}40%{transform:scale(1.4);background:var(--c)}}
#toast{position:fixed;bottom:28px;left:50%;transform:translateX(-50%) translateY(80px);z-index:9999;padding:10px 24px;border-radius:99px;font-size:.875rem;font-weight:600;background:rgba(10,10,10,.95);border:1px solid;transition:transform .35s ease;font-family:'JetBrains Mono',monospace;white-space:nowrap}
#toast.show{transform:translateX(-50%) translateY(0)}
#toast.success{border-color:rgba(74,222,128,.4);color:var(--green)}
#toast.error{border-color:rgba(255,68,68,.4);color:var(--red)}
#toast.info{border-color:rgba(200,216,228,.4);color:var(--c)}
.inspection-results-banner{display:none;animation:pfade .5s ease;margin-bottom:24px}
.inspection-results-banner.visible{display:block}
.res-header{background:linear-gradient(135deg,rgba(200,216,228,.1),rgba(200,216,228,.04));padding:32px 36px;border-radius:20px;margin-bottom:24px;border:2px solid rgba(200,216,228,.25);position:relative;overflow:hidden}
.res-header-top{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:20px}
.res-header h2{font-size:1.8rem;font-weight:800;color:#fff;margin-bottom:6px;letter-spacing:-.02em}
.res-header p{color:#9ca3af;font-size:.875rem}
.res-check-icon{width:48px;height:48px;background:rgba(74,222,128,.15);border:1.5px solid rgba(74,222,128,.4);border-radius:14px;display:flex;align-items:center;justify-content:center;flex-shrink:0;animation:pop .4s cubic-bezier(.34,1.56,.64,1)}
@keyframes pop{0%{transform:scale(0);opacity:0}100%{transform:scale(1);opacity:1}}
.res-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
.res-stat-card{background:rgba(0,0,0,.5);padding:18px 16px;border-radius:14px;text-align:center;border:1px solid rgba(255,255,255,.08)}
.res-stat-val{font-size:2.2rem;font-weight:800;color:#fff;margin-bottom:4px;line-height:1;font-family:'JetBrains Mono',monospace}
.res-stat-val.green{color:var(--green)}.res-stat-val.yellow{color:var(--yellow)}.res-stat-val.red{color:var(--red)}
.res-stat-lbl{color:#6b7280;font-size:.75rem;text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace}
.res-gallery-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(340px,1fr));gap:20px}
.res-gallery-item{border-radius:14px;overflow:hidden;border:2px solid rgba(200,216,228,.2);background:rgba(0,0,0,.4);transition:all .35s;position:relative;box-shadow:0 4px 20px rgba(0,0,0,.4)}
.res-gallery-item:hover{border-color:rgba(200,216,228,.6);transform:translateY(-4px)}
.res-gallery-item img{width:100%;display:block;aspect-ratio:16/10;object-fit:cover}
.res-gallery-placeholder{aspect-ratio:16/10;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;color:#4b5563;font-size:.85rem;background:rgba(255,255,255,.02)}
.res-gallery-placeholder span{font-size:3rem;opacity:.4}
.res-gallery-badge{position:absolute;bottom:10px;left:10px;background:rgba(0,0,0,.85);border:1px solid rgba(200,216,228,.3);border-radius:8px;padding:5px 12px;font-size:.78rem;font-family:'JetBrains Mono',monospace;color:var(--c);backdrop-filter:blur(6px)}
.res-gallery-dismiss{position:absolute;top:10px;right:10px;width:32px;height:32px;background:rgba(0,0,0,.72);border:1.5px solid rgba(255,255,255,.2);border-radius:50%;display:flex;align-items:center;justify-content:center;cursor:pointer;color:#fff;font-size:15px;transition:all .2s;backdrop-filter:blur(6px);opacity:0;z-index:10}
.res-gallery-item:hover .res-gallery-dismiss{opacity:1}
.res-gallery-dismiss:hover{background:rgba(255,68,68,.85);border-color:rgba(255,68,68,.7);transform:scale(1.15)}
.res-gallery-item.crossed{opacity:.38;filter:grayscale(1);border-color:rgba(255,68,68,.4)!important;box-shadow:none!important;transform:none!important}
.res-gallery-item.crossed .res-gallery-dismiss{opacity:1;background:rgba(255,68,68,.8);border-color:rgba(255,68,68,.6)}
.res-gallery-item.crossed .res-gallery-dismiss:hover{background:rgba(74,222,128,.8);border-color:rgba(74,222,128,.6)}
.res-cross-overlay{position:absolute;inset:0;pointer-events:none;opacity:0;transition:opacity .22s}
.res-gallery-item.crossed .res-cross-overlay{opacity:1}
.res-cross-overlay svg{width:100%;height:100%}
.res-defect-grid{display:flex;flex-direction:column;gap:8px}
.res-defect-row{display:flex;align-items:center;gap:12px;background:rgba(255,255,255,.025);border:1px solid rgba(255,255,255,.06);border-radius:10px;padding:10px 14px}
.res-defect-name{font-weight:600;font-size:.875rem;min-width:140px}
.res-defect-bar-wrap{flex:1;height:4px;background:rgba(255,255,255,.06);border-radius:2px;overflow:hidden}
.res-defect-bar{height:100%;border-radius:2px;background:linear-gradient(90deg,var(--c),#8aabb8);transition:width 1s cubic-bezier(.4,0,.2,1)}
.res-defect-conf{font-size:.8rem;font-family:'JetBrains Mono',monospace;color:var(--tm);min-width:40px;text-align:right}
.res-no-defects{text-align:center;padding:28px;background:rgba(74,222,128,.04);border:1px solid rgba(74,222,128,.2);border-radius:12px;color:var(--green);font-weight:700}
.res-cta-banner{background:linear-gradient(135deg,rgba(200,216,228,.08),rgba(138,171,184,.06));border:1.5px solid rgba(200,216,228,.25);border-radius:16px;padding:24px 28px;display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap;margin-bottom:24px}
.res-cta-text h4{font-size:1rem;font-weight:700;color:#fff;margin-bottom:4px}
.res-cta-text p{font-size:.85rem;color:var(--tm)}
.res-cta-actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center}
.audio-drop-zone{border:2px dashed rgba(200,216,228,.2);border-radius:16px;padding:36px 24px 28px;text-align:center;cursor:pointer;transition:all .3s;background:rgba(200,216,228,.02)}
.audio-drop-zone:hover,.audio-drop-zone.drag{border-color:rgba(200,216,228,.5);background:rgba(200,216,228,.05)}
.adz-icon{width:56px;height:56px;background:rgba(200,216,228,.08);border:1px solid rgba(200,216,228,.25);border-radius:14px;display:flex;align-items:center;justify-content:center;margin:0 auto 14px;color:var(--c);transition:all .3s}
.audio-drop-zone:hover .adz-icon{background:rgba(200,216,228,.18);transform:scale(1.08)}
.adz-text p{color:var(--t);font-size:.95rem;margin-bottom:4px;font-weight:600}
.adz-text span{font-size:.8rem;color:var(--td);font-family:'JetBrains Mono',monospace}
.adz-bars{display:flex;align-items:flex-end;gap:3px;height:32px;justify-content:center;margin-top:20px;opacity:.35}
.adz-bar{width:6px;border-radius:3px;background:linear-gradient(to top,#8aabb8,#c8d8e4);flex-shrink:0}
.audio-file-card{display:flex;align-items:center;gap:14px;padding:14px 18px;background:rgba(200,216,228,.05);border:1px solid rgba(200,216,228,.2);border-radius:12px}
.afc-icon{width:40px;height:40px;background:rgba(200,216,228,.1);border-radius:10px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.afc-info{flex:1;min-width:0}
.afc-name{font-weight:700;font-size:.9rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;color:var(--t)}
.afc-meta{font-size:.75rem;color:var(--tm);font-family:'JetBrains Mono',monospace;margin-top:2px}
.afc-remove{width:28px;height:28px;background:rgba(255,68,68,.1);border:1px solid rgba(255,68,68,.25);border-radius:50%;display:flex;align-items:center;justify-content:center;cursor:pointer;color:var(--red);font-size:12px;flex-shrink:0;transition:all .2s}
.afc-remove:hover{background:rgba(255,68,68,.25)}
.audio-tabs{display:flex;gap:0;background:rgba(0,0,0,.4);border-radius:10px;padding:4px;margin-bottom:20px;width:fit-content}
.audio-tab{padding:8px 20px;border-radius:8px;font-weight:600;font-size:.875rem;cursor:pointer;border:none;font-family:'Syne',sans-serif;color:var(--tm);background:transparent;transition:all .25s}
.audio-tab.active{background:linear-gradient(135deg,rgba(200,216,228,.25),rgba(138,171,184,.2));color:var(--c)}
.live-rec-zone{border:2px solid rgba(200,216,228,.2);border-radius:16px;padding:32px 24px;text-align:center;background:rgba(200,216,228,.02);transition:all .3s}
.live-rec-zone.recording{border-color:rgba(255,68,68,.5);background:rgba(255,68,68,.03);animation:recpulse 2s ease-in-out infinite}
@keyframes recpulse{0%,100%{box-shadow:0 0 0 0 rgba(255,68,68,.1)}50%{box-shadow:0 0 0 12px rgba(255,68,68,.0)}}
.rec-timer{font-size:3rem;font-weight:800;color:var(--red);font-family:'JetBrains Mono',monospace;margin:16px 0;letter-spacing:.05em}
.rec-meter{height:6px;background:rgba(255,255,255,.06);border-radius:3px;overflow:hidden;margin:12px 0}
.rec-meter-fill{height:100%;background:linear-gradient(90deg,var(--green),var(--yellow),var(--red));border-radius:3px;width:0%;transition:width .1s}
.rec-status-dot{display:inline-block;width:10px;height:10px;border-radius:50%;background:var(--red);animation:blink 1s ease infinite;margin-right:8px;vertical-align:middle}
.rec-waves{display:flex;align-items:center;gap:3px;justify-content:center;height:40px;margin:12px 0}
.rec-wave-bar{width:4px;border-radius:2px;background:var(--red);transition:height .1s ease;min-height:4px}
.audio-player-wrap{background:rgba(0,0,0,.45);border:1px solid rgba(200,216,228,.18);border-radius:12px;padding:16px 20px;margin-top:14px}
.apc-label{font-size:.75rem;font-weight:700;color:var(--c);text-transform:uppercase;letter-spacing:.07em;font-family:'JetBrains Mono',monospace;margin-bottom:8px;display:flex;align-items:center;gap:6px}
.audio-loader-card{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.15);border-radius:18px;padding:32px 28px;margin-bottom:24px}
.alc-header{display:flex;align-items:center;gap:16px;margin-bottom:28px}
.alc-icon-ring{width:56px;height:56px;border-radius:50%;border:2px solid rgba(200,216,228,.3);display:flex;align-items:center;justify-content:center;flex-shrink:0;animation:ringpulse 2s ease-in-out infinite}
@keyframes ringpulse{0%,100%{box-shadow:0 0 10px rgba(200,216,228,.1)}50%{box-shadow:0 0 24px rgba(200,216,228,.35)}}
.alc-title{font-size:1.1rem;font-weight:700;color:#fff;margin-bottom:4px}
.alc-sub{font-size:.8rem;color:var(--tm);font-family:'JetBrains Mono',monospace}
.alc-wave-wrap{position:relative;background:rgba(0,0,0,.5);border:1px solid rgba(200,216,228,.1);border-radius:12px;overflow:hidden;height:80px;margin-bottom:24px}
.alc-wave-canvas{width:100%;height:100%;display:block}
.alc-scan-line{position:absolute;top:0;bottom:0;width:2px;background:linear-gradient(to bottom,transparent,var(--c),rgba(255,255,255,.9),var(--c),transparent);animation:alcScan 1.6s linear infinite;filter:blur(1px)}
@keyframes alcScan{0%{left:-2px;opacity:0}5%{opacity:1}95%{opacity:1}100%{left:100%;opacity:0}}
.alc-steps{display:flex;flex-direction:column;gap:10px;margin-bottom:20px}
.alc-step{display:flex;align-items:center;gap:12px;font-size:.85rem;color:var(--td);transition:all .4s}
.alc-step.active{color:var(--t)}.alc-step.done{color:var(--green)}
.alc-step-dot{width:8px;height:8px;border-radius:50%;background:rgba(255,255,255,.15);flex-shrink:0;transition:all .4s}
.alc-step.active .alc-step-dot{background:var(--c);box-shadow:0 0 8px rgba(200,216,228,.6);animation:dotpulse 1s ease infinite}
.alc-step.done .alc-step-dot{background:var(--green);box-shadow:none;animation:none}
.alc-progress-wrap{display:flex;align-items:center;gap:12px}
.alc-progress-track{flex:1;height:3px;background:rgba(200,216,228,.1);border-radius:99px;overflow:hidden}
.alc-progress-fill{height:100%;background:linear-gradient(90deg,#8aabb8,#c8d8e4);border-radius:99px;width:0%;transition:width .6s ease}
.alc-progress-pct{font-family:'JetBrains Mono',monospace;font-size:.75rem;color:var(--tm);min-width:36px;text-align:right}
.audio-result-rich{border-radius:18px;border:2px solid;overflow:hidden;margin-bottom:24px;animation:pfade .4s ease}
.audio-result-rich.knock{border-color:rgba(255,68,68,.4)}.audio-result-rich.clean{border-color:rgba(74,222,128,.4)}
.arr-header{padding:24px 28px;display:flex;align-items:center;gap:18px}
.audio-result-rich.knock .arr-header{background:linear-gradient(135deg,rgba(255,68,68,.12),rgba(255,68,68,.05))}
.audio-result-rich.clean .arr-header{background:linear-gradient(135deg,rgba(74,222,128,.12),rgba(74,222,128,.05))}
.arr-verdict-badge{width:60px;height:60px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:1.8rem;flex-shrink:0;border:2px solid}
.audio-result-rich.knock .arr-verdict-badge{background:rgba(255,68,68,.15);border-color:rgba(255,68,68,.4)}
.audio-result-rich.clean .arr-verdict-badge{background:rgba(74,222,128,.15);border-color:rgba(74,222,128,.4)}
.arr-verdict-text h3{font-size:1.3rem;font-weight:800;margin-bottom:4px}
.audio-result-rich.knock .arr-verdict-text h3{color:var(--red)}
.audio-result-rich.clean .arr-verdict-text h3{color:var(--green)}
.arr-verdict-text p{font-size:.85rem;color:var(--tm)}
.arr-body{padding:20px 28px;border-top:1px solid rgba(255,255,255,.06)}
.arr-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:20px}
.arr-metric{background:rgba(0,0,0,.3);border:1px solid rgba(255,255,255,.06);border-radius:10px;padding:14px 12px;text-align:center}
.arr-metric-val{font-size:1.4rem;font-weight:800;font-family:'JetBrains Mono',monospace;color:var(--c);margin-bottom:4px}
.arr-metric-lbl{font-size:.7rem;text-transform:uppercase;letter-spacing:.08em;color:var(--td);font-family:'JetBrains Mono',monospace}
.arr-scores-title{font-size:.75rem;font-weight:700;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:10px}
.arr-score-row{display:flex;align-items:center;gap:10px;margin-bottom:7px}
.arr-score-label{font-size:.8rem;font-family:'JetBrains Mono',monospace;color:var(--t);min-width:140px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.arr-score-bar{flex:1;height:4px;background:rgba(255,255,255,.06);border-radius:2px;overflow:hidden}
.arr-score-fill{height:100%;border-radius:2px;transition:width .9s ease}
.audio-result-rich.knock .arr-score-fill{background:linear-gradient(90deg,#ff6666,var(--red))}
.audio-result-rich.clean .arr-score-fill{background:linear-gradient(90deg,#6ee7b7,var(--green))}
.arr-score-pct{font-size:.75rem;font-family:'JetBrains Mono',monospace;color:var(--tm);min-width:36px;text-align:right}
.pdf-choice-box{background:rgba(200,216,228,.04);border:1px solid rgba(200,216,228,.2);border-radius:14px;padding:20px 24px;margin-bottom:20px}
.pdf-choice-box h4{font-size:.9rem;font-weight:700;color:var(--c);margin-bottom:12px;display:flex;align-items:center;gap:8px;font-family:'JetBrains Mono',monospace;text-transform:uppercase;letter-spacing:.06em}
.pdf-choice-toggle{display:flex;gap:0;background:rgba(0,0,0,.4);border-radius:8px;overflow:hidden;border:1px solid rgba(200,216,228,.15);width:fit-content}
.pdf-choice-opt{padding:8px 20px;font-size:.85rem;font-weight:600;cursor:pointer;border:none;font-family:'Syne',sans-serif;transition:all .25s;color:var(--tm);background:transparent}
.pdf-choice-opt.active{background:linear-gradient(135deg,rgba(200,216,228,.2),rgba(138,171,184,.2));color:var(--c)}
#pdfViewerFrame{width:100%;height:900px;border:none;border-radius:16px;border:1px solid rgba(200,216,228,.15)}
.results-panel{animation:pfade .5s ease}
.result-hero{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin-bottom:24px}
.verdict-box{padding:28px;border-radius:16px;text-align:center;border:2px solid}
.verdict-box.pass{border-color:rgba(74,222,128,.4);background:rgba(74,222,128,.06)}
.verdict-box.attention{border-color:rgba(245,158,11,.4);background:rgba(245,158,11,.06)}
.verdict-box.fail{border-color:rgba(255,68,68,.4);background:rgba(255,68,68,.06)}
.verdict-label{font-size:.75rem;font-weight:700;text-transform:uppercase;letter-spacing:.1em;font-family:'JetBrains Mono',monospace;color:var(--tm);margin-bottom:8px}
.verdict-status{font-size:2.5rem;font-weight:800;letter-spacing:-.02em}
.verdict-box.pass .verdict-status{color:var(--green)}.verdict-box.attention .verdict-status{color:var(--yellow)}.verdict-box.fail .verdict-status{color:var(--red)}
.verdict-sub{font-size:.85rem;color:var(--tm);margin-top:6px}
.defect-list{display:flex;flex-direction:column;gap:10px}
.ditem{display:flex;align-items:center;padding:10px 16px;background:rgba(255,255,255,.03);border-radius:10px;border:1px solid rgba(255,255,255,.06)}
.ditem-name{font-weight:600;font-size:.9rem}
.ditem-bar{height:4px;background:rgba(255,255,255,.06);border-radius:2px;flex:1;margin:0 12px}
.ditem-fill{height:100%;border-radius:2px;background:linear-gradient(90deg,var(--c),#8aabb8);transition:width .8s ease}
.ditem-conf{font-size:.8rem;font-family:'JetBrains Mono',monospace;color:var(--tm)}
.ann-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:14px;margin-top:16px}
.ann-img{border-radius:10px;overflow:hidden;border:1px solid rgba(200,216,228,.2);position:relative;transition:all .3s}
.ann-img:hover{border-color:rgba(200,216,228,.5);transform:scale(1.02)}
.ann-img img{width:100%;display:block}
.ann-img-label{position:absolute;bottom:0;left:0;right:0;padding:8px 12px;background:linear-gradient(to top,rgba(0,0,0,.9),transparent);font-size:.75rem;color:var(--c);font-family:'JetBrains Mono',monospace}
#live-camera{padding-top:80px;min-height:100vh}
.live-wrap{max-width:1000px;margin:0 auto;padding:40px 24px}
.live-hd{margin-bottom:36px}
.live-hd h1{font-size:2.2rem;font-weight:800;letter-spacing:-.03em;background:linear-gradient(90deg,#fff,var(--c));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:8px}
.live-hd p{color:var(--tm);font-size:1rem}
.setup-box{background:rgba(200,216,228,.04);border:1px solid rgba(200,216,228,.18);border-radius:12px;padding:18px 22px;margin-bottom:20px}
.setup-box h5{font-size:.85rem;font-weight:700;color:var(--c);margin-bottom:10px;font-family:'JetBrains Mono',monospace;text-transform:uppercase;letter-spacing:.06em}
.setup-box p{color:var(--tm);font-size:.875rem;line-height:1.9}
.live-camera-card{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:18px;padding:28px;margin-bottom:24px}
.video-wrap{position:relative;border-radius:16px;overflow:hidden;margin-bottom:20px;border:2px solid rgba(200,216,228,.3);background:#000}
.video-wrap video{width:100%;display:block;background:#000;min-height:300px}
.cam-hud{position:absolute;top:16px;left:16px;background:rgba(0,0,0,.82);border:1px solid rgba(200,216,228,.25);border-radius:12px;padding:14px 18px;font-family:'JetBrains Mono',monospace;font-size:.78rem;color:#fff;backdrop-filter:blur(8px);min-width:160px}
.cam-hud .hud-row{margin-bottom:6px;color:var(--tm)}.cam-hud .hud-row:last-child{margin-bottom:0}.cam-hud .hud-row strong{color:#fff}
.live-dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:var(--red);animation:blink 1s ease infinite;margin-right:6px;vertical-align:middle}
@keyframes blink{0%,100%{opacity:1}50%{opacity:.3}}
.capture-badge{position:absolute;top:16px;right:16px;background:linear-gradient(135deg,#c8d8e4,#8aabb8);color:#0a0f12;padding:10px 24px;border-radius:99px;font-weight:700;font-size:.875rem;font-family:'JetBrains Mono',monospace;animation:captureFlash .7s ease forwards;pointer-events:none}
@keyframes captureFlash{0%{opacity:0;transform:scale(.8)}50%{opacity:1;transform:scale(1.1)}100%{opacity:1;transform:scale(1)}}
.cam-controls-row{display:flex;gap:12px;justify-content:center;align-items:center;flex-wrap:wrap}
.defect-tags{display:flex;flex-wrap:wrap;gap:8px;margin-top:16px}
.dtag{padding:4px 12px;border-radius:99px;font-size:.78rem;font-weight:600;font-family:'JetBrains Mono',monospace;background:rgba(255,68,68,.12);border:1px solid rgba(255,68,68,.3);color:#ff8888}
.dtag.new{animation:tagpop .4s ease;background:rgba(200,216,228,.12);border-color:rgba(200,216,228,.4);color:var(--c)}
@keyframes tagpop{0%{transform:scale(0.7);opacity:0}100%{transform:scale(1);opacity:1}}
.live-stats-row{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:24px}
.lstat{background:rgba(0,0,0,.4);border:1px solid rgba(200,216,228,.12);border-radius:12px;padding:16px;text-align:center}
.lstat-val{font-size:1.8rem;font-weight:800;color:var(--c);margin-bottom:4px}
.lstat-lbl{font-size:.7rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace}
#history{padding-top:80px;min-height:100vh}
.history-wrap{max-width:1300px;margin:0 auto;padding:40px 24px}
.history-hd{margin-bottom:40px}
.history-hd h1{font-size:2.4rem;font-weight:800;letter-spacing:-.03em;background:linear-gradient(90deg,#fff,var(--c));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:8px}
.history-hd p{color:var(--tm);font-size:1rem}
.history-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:20px;margin-bottom:36px}
.hstat-card{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:16px;padding:24px 20px;display:flex;align-items:center;gap:16px;transition:all .3s}
.hstat-card:hover{border-color:rgba(200,216,228,.3);transform:translateY(-3px)}
.hstat-icon{width:48px;height:48px;border-radius:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.hstat-icon.blue{background:rgba(200,216,228,.15)}.hstat-icon.green{background:rgba(74,222,128,.15)}.hstat-icon.yellow{background:rgba(245,158,11,.15)}
.hstat-val{font-size:2rem;font-weight:800;color:#fff;margin-bottom:2px}
.hstat-lbl{font-size:.8rem;color:var(--tm);font-family:'JetBrains Mono',monospace}
.history-search-row{display:flex;gap:12px;margin-bottom:28px;align-items:center}
.history-search-wrap{flex:1;position:relative}
.history-search-wrap svg{position:absolute;left:14px;top:50%;transform:translateY(-50%);color:var(--td)}
.history-search{width:100%;background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);border-radius:10px;padding:10px 14px 10px 44px;color:var(--t);font-family:'Syne',sans-serif;font-size:.9rem;outline:none;transition:border-color .2s}
.history-search:focus{border-color:rgba(200,216,228,.5)}.history-search::placeholder{color:var(--td)}
.inspection-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px}
.insp-card{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.12);border-radius:18px;overflow:hidden;transition:all .35s}
.insp-card:hover{border-color:rgba(200,216,228,.35);transform:translateY(-6px)}
.insp-card-img{position:relative;height:192px;overflow:hidden}
.insp-card-img img{width:100%;height:100%;object-fit:cover;transition:transform .4s}
.insp-card:hover .insp-card-img img{transform:scale(1.05)}
.insp-card-img-overlay{position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,.85) 0%,transparent 100%)}
.insp-status-badge{position:absolute;top:12px;right:12px;padding:4px 12px;border-radius:99px;font-size:.75rem;font-weight:700;border:1px solid;font-family:'JetBrains Mono',monospace;display:flex;align-items:center;gap:6px}
.insp-status-badge.pass{background:rgba(74,222,128,.12);color:var(--green);border-color:rgba(74,222,128,.3)}
.insp-status-badge.attention{background:rgba(245,158,11,.12);color:var(--yellow);border-color:rgba(245,158,11,.3)}
.insp-status-badge.fail{background:rgba(255,68,68,.12);color:var(--red);border-color:rgba(255,68,68,.3)}
.insp-content{padding:20px}
.insp-vehicle{font-size:1rem;font-weight:700;color:#fff;margin-bottom:4px}
.insp-vin{font-size:.78rem;color:var(--td);font-family:'JetBrains Mono',monospace;margin-bottom:12px}
.insp-meta{display:flex;align-items:center;gap:6px;font-size:.82rem;color:var(--tm);margin-bottom:14px}
.insp-nums{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:14px}
.insp-num-box{background:rgba(0,0,0,.3);border-radius:8px;padding:10px 12px;text-align:center}
.insp-num{font-size:1.4rem;font-weight:800;color:var(--c);margin-bottom:2px}
.insp-num-lbl{font-size:.7rem;color:var(--td);font-family:'JetBrains Mono',monospace}
.history-empty{text-align:center;padding:80px 24px;color:var(--tm);grid-column:1/-1}
.history-empty-icon{font-size:3.5rem;margin-bottom:20px;opacity:.4}
.history-empty h3{font-size:1.3rem;font-weight:700;color:var(--t);margin-bottom:10px}
.history-empty p{font-size:.9rem;max-width:400px;margin:0 auto 24px}
.btn-danger-sm{background:rgba(255,68,68,.1);color:var(--red);border:1px solid rgba(255,68,68,.25);padding:8px 16px;border-radius:8px;font-size:.8rem;font-weight:600;cursor:pointer;font-family:'Syne',sans-serif;transition:all .2s;display:inline-flex;align-items:center;gap:6px}
.btn-danger-sm:hover{background:rgba(255,68,68,.2)}
#signup{padding-top:80px;min-height:100vh}
.auth-wrap{max-width:1200px;margin:0 auto;padding:60px 24px;display:grid;grid-template-columns:1fr 1fr;gap:64px;align-items:center}
.auth-left h1{font-size:clamp(2rem,4vw,3rem);font-weight:800;line-height:1.1;letter-spacing:-.03em;margin-bottom:20px}
.auth-left h1 .al1{background:linear-gradient(90deg,#fff,rgba(255,255,255,.8));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.auth-left h1 .al2{background:linear-gradient(90deg,var(--c),#a0b8c8);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.auth-left p{color:var(--tm);font-size:1rem;line-height:1.7;margin-bottom:28px}
.auth-features{display:flex;flex-direction:column;gap:14px}
.auth-feat-item{display:flex;align-items:center;gap:12px;font-size:.9rem;color:#d1d5db}
.auth-feat-item svg{color:var(--c);flex-shrink:0}
.auth-card{background:rgba(255,255,255,.025);border:1px solid rgba(200,216,228,.15);border-radius:20px;padding:36px;backdrop-filter:blur(10px)}
.auth-tabs{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:28px;background:rgba(0,0,0,.4);border-radius:10px;padding:4px}
.auth-tab{padding:10px 20px;border-radius:8px;font-weight:700;font-size:.875rem;cursor:pointer;border:none;font-family:'Syne',sans-serif;color:var(--tm);background:transparent;width:100%;transition:all .25s}
.auth-tab.active{background:linear-gradient(135deg,#c8d8e4,#8aabb8);color:#0a0f12}
.auth-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:6px}
.auth-form-group{display:flex;flex-direction:column;gap:6px;margin-bottom:16px}
.auth-form-group label{font-size:.78rem;font-weight:600;color:var(--tm);text-transform:uppercase;letter-spacing:.06em;font-family:'JetBrains Mono',monospace}
.auth-form-group input{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:10px 14px;color:var(--t);font-family:'Syne',sans-serif;font-size:.9rem;outline:none;transition:border-color .2s;width:100%}
.auth-form-group input:focus{border-color:rgba(200,216,228,.5);background:rgba(200,216,228,.04)}
.auth-form-group input::placeholder{color:var(--td)}
.auth-hint{font-size:.75rem;color:var(--td);margin-top:4px;font-family:'JetBrains Mono',monospace}
.auth-submit{width:100%;justify-content:center;padding:12px 20px;font-size:.95rem;margin-bottom:20px}
.auth-toggle{text-align:center;font-size:.85rem;color:var(--tm)}
.auth-toggle a{color:var(--c);cursor:pointer;font-weight:700;margin-left:4px}
.auth-divider{margin-top:24px;padding-top:20px;border-top:1px solid rgba(200,216,228,.12);text-align:center;font-size:.75rem;color:var(--td);font-family:'JetBrains Mono',monospace}
.auth-error{background:rgba(255,68,68,.1);border:1px solid rgba(255,68,68,.3);border-radius:8px;padding:10px 16px;font-size:.85rem;color:#ff8888;margin-bottom:14px;display:none}
.auth-error.show{display:block}
#about{padding-top:80px}
.about-wrap{max-width:900px;margin:0 auto;padding:60px 24px}
.about-wrap h1{font-size:3rem;font-weight:800;background:linear-gradient(90deg,#fff,var(--c));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:20px}
.about-wrap p{color:var(--tm);font-size:1.05rem;line-height:1.8;margin-bottom:20px}
.about-feat{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:40px}
.af{padding:24px;background:rgba(255,255,255,.03);border:1px solid rgba(200,216,228,.12);border-radius:14px}
.af h4{font-weight:700;margin-bottom:8px;color:var(--c)}
.af p{font-size:.9rem;color:var(--tm);line-height:1.6}
.back-link{display:inline-flex;align-items:center;gap:6px;color:var(--tm);font-size:.875rem;cursor:pointer;transition:color .2s;margin-bottom:32px;font-weight:600}
.back-link:hover{color:var(--c)}
.action-row{display:flex;gap:12px;align-items:center;flex-wrap:wrap}
/* ── AI Health Panel ─────────────────────────────────────── */
.ai-panel{background:linear-gradient(135deg,rgba(15,23,42,.95),rgba(15,23,42,.85));border:1.5px solid rgba(200,216,228,.25);border-radius:20px;overflow:hidden;margin-bottom:24px;animation:pfade .5s ease}
.ai-panel-header{padding:20px 28px 16px;background:linear-gradient(90deg,rgba(200,216,228,.08),transparent);border-bottom:1px solid rgba(200,216,228,.1);display:flex;align-items:center;gap:12px}
.ai-panel-badge{background:linear-gradient(135deg,#c8d8e4,#8aabb8);color:#0a0f12;font-size:.7rem;font-weight:800;padding:3px 10px;border-radius:99px;font-family:'JetBrains Mono',monospace;letter-spacing:.06em;text-transform:uppercase;flex-shrink:0}
.ai-panel-title{font-size:1rem;font-weight:700;color:#fff}
.ai-panel-body{padding:24px 28px;display:grid;grid-template-columns:1fr 1fr;gap:20px}
@media(max-width:768px){.ai-panel-body{grid-template-columns:1fr}}
.health-score-wrap{grid-column:1/-1;background:rgba(0,0,0,.35);border-radius:14px;padding:20px 24px;display:flex;align-items:center;gap:24px}
.health-score-ring{position:relative;width:90px;height:90px;flex-shrink:0}
.health-score-ring svg{width:90px;height:90px;transform:rotate(-90deg)}
.hsr-track{fill:none;stroke:rgba(255,255,255,.07);stroke-width:8}
.hsr-fill{fill:none;stroke-width:8;stroke-linecap:round;transition:stroke-dashoffset 1.2s cubic-bezier(.4,0,.2,1)}
.health-score-val{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center}
.hsv-num{font-size:1.4rem;font-weight:800;font-family:'JetBrains Mono',monospace;line-height:1}
.hsv-lbl{font-size:.55rem;color:var(--tm);text-transform:uppercase;letter-spacing:.06em;margin-top:2px}
.health-score-info{flex:1}
.hsi-label{font-size:.75rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:6px}
.hsi-bar{height:8px;background:rgba(255,255,255,.06);border-radius:4px;overflow:hidden;margin-bottom:8px}
.hsi-bar-fill{height:100%;border-radius:4px;transition:width 1.2s cubic-bezier(.4,0,.2,1)}
.hsi-desc{font-size:.82rem;color:var(--tm)}
.risk-card{background:rgba(0,0,0,.3);border-radius:12px;padding:16px 20px;border:1px solid rgba(255,255,255,.06)}
.risk-card-label{font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;color:var(--tm);margin-bottom:10px}
.risk-badge-lg{display:inline-flex;align-items:center;gap:8px;padding:8px 18px;border-radius:10px;font-weight:700;font-size:1rem;border:1.5px solid;margin-bottom:8px}
.risk-badge-lg.low{background:rgba(74,222,128,.1);border-color:rgba(74,222,128,.4);color:#4ade80}
.risk-badge-lg.medium{background:rgba(245,158,11,.1);border-color:rgba(245,158,11,.4);color:#f59e0b}
.risk-badge-lg.high{background:rgba(249,115,22,.1);border-color:rgba(249,115,22,.4);color:#fb923c}
.risk-badge-lg.critical{background:rgba(255,68,68,.12);border-color:rgba(255,68,68,.4);color:#ff4444}
.risk-factors-list{display:flex;flex-direction:column;gap:5px}
.rf-item{font-size:.8rem;color:#fca5a5;display:flex;align-items:flex-start;gap:6px;line-height:1.4}
.rf-item::before{content:"⚠";font-size:.7rem;flex-shrink:0;margin-top:1px}
.ai-summary-box{grid-column:1/-1;background:linear-gradient(135deg,rgba(30,58,138,.2),rgba(30,64,175,.1));border:1px solid rgba(96,165,250,.2);border-radius:12px;padding:18px 20px}
.ai-summary-label{font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.1em;font-family:'JetBrains Mono',monospace;color:#93c5fd;margin-bottom:10px;display:flex;align-items:center;gap:6px}
.ai-summary-text{font-size:.9rem;color:#e2e8f0;line-height:1.7}
.ai-recs-box{background:rgba(0,0,0,.3);border-radius:12px;padding:16px 20px;border:1px solid rgba(255,255,255,.06)}
.ai-recs-label{font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;color:var(--tm);margin-bottom:10px;display:flex;align-items:center;gap:6px}
.ai-rec-item{display:flex;align-items:flex-start;gap:10px;padding:8px 0;border-bottom:1px solid rgba(255,255,255,.04);font-size:.85rem;color:#d1d5db;line-height:1.5}
.ai-rec-item:last-child{border-bottom:none}
.ai-rec-num{width:22px;height:22px;border-radius:50%;background:rgba(200,216,228,.12);border:1px solid rgba(200,216,228,.25);display:flex;align-items:center;justify-content:center;font-size:.7rem;font-weight:700;font-family:'JetBrains Mono',monospace;color:var(--c);flex-shrink:0}
.ai-loading-state{padding:32px;text-align:center;color:var(--tm);display:flex;flex-direction:column;align-items:center;gap:12px}
.ai-loading-state .spinner-light{width:28px;height:28px}
.ai-loading-state p{font-size:.875rem;font-family:'JetBrains Mono',monospace}
@media(max-width:1024px){.hero-inner{grid-template-columns:1fr}.stats-row{grid-template-columns:repeat(2,1fr)}.feat-grid,.steps-grid{grid-template-columns:repeat(2,1fr)}.form-grid{grid-template-columns:repeat(2,1fr)}.result-hero{grid-template-columns:1fr}.auth-wrap{grid-template-columns:1fr}.inspection-grid{grid-template-columns:repeat(2,1fr)}.res-stats{grid-template-columns:repeat(3,1fr)}}
@media(max-width:768px){.stats-row,.feat-grid,.steps-grid{grid-template-columns:1fr}.prog-steps{flex-direction:column}.pstep{border-right:none;border-bottom:1px solid rgba(200,216,228,.08);justify-content:flex-start;padding:12px 20px}.form-grid,.about-feat,.auth-form-grid{grid-template-columns:1fr}.foot-inner,.history-stats,.inspection-grid{grid-template-columns:1fr}.res-stats,.live-stats-row{grid-template-columns:1fr}.result-hero{grid-template-columns:1fr}.res-cta-banner{flex-direction:column;align-items:flex-start}.res-gallery-grid{grid-template-columns:1fr}.auth-wrap{gap:40px}.arr-metrics{grid-template-columns:1fr 1fr}}
</style>
</head>
<body>
<div class="bg"><div class="bg-grid"></div><div class="bg-orb a"></div><div class="bg-orb b"></div></div>

<div id="scanOverlay">
  <div class="scan-ol-logo">⬡ AutoSpect AI</div>
  <div class="scan-img-strip" id="scanImgStrip"></div>
  <div class="scan-status-box"><div class="scan-status-label">AI Vision Processing</div><div class="scan-status-msg" id="scanStatusMsg">Initializing scan...</div></div>
  <div class="scan-progress-wrap"><div class="scan-progress-track"><div class="scan-progress-fill" id="scanProgressFill"></div></div><div class="scan-progress-pct" id="scanProgressPct">0%</div></div>
  <div class="scan-dots"><div class="scan-dot"></div><div class="scan-dot"></div><div class="scan-dot"></div></div>
</div>

<nav>
  <div class="nav-inner">
    <div class="logo" onclick="goTo('landing')">
      <img src="/static/carai.png" alt="AutoSpect AI" style="height:38px;width:auto;display:block" onerror="this.style.display='none'">
      <span class="logo-name">AutoSpect AI</span>
    </div>
    <div class="nav-pills">
      <div id="navUserInfo"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg><span id="navUserEmail"></span><button onclick="signOutUser()" style="background:rgba(255,68,68,.12);border:1px solid rgba(255,68,68,.3);color:#ff8888;padding:3px 10px;border-radius:6px;font-size:.75rem;font-family:'Syne',sans-serif;cursor:pointer;font-weight:600">Sign Out</button></div>
      <button class="nav-pill ghost" onclick="goTo('about')">About</button>
      <button class="nav-pill ghost" onclick="goTo('history')">History</button>
      <button class="nav-pill ghost" id="navAuthBtn" onclick="goTo('signup')">Sign Up</button>
      <button class="nav-pill cta" onclick="goTo('dashboard')">Start Inspection</button>
    </div>
  </div>
</nav>
<div id="toast"></div>

<!-- LANDING -->
<div id="landing" class="page active">
  <section class="hero">
    <div style="max-width:1300px;margin:0 auto;width:100%">
      <div class="hero-inner">
        <div>
          <div class="hero-label"><svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>NEXT-GEN AI TECHNOLOGY</div>
          <h1><span class="line1">Vehicle Inspection</span><br><span class="line2">Reimagined</span></h1>
          <p class="hero-sub">AI-driven damage detection, engine knock analysis, and professional PDF reporting — in under 2 minutes.</p>
          <div class="hero-btns">
            <button class="btn btn-primary" onclick="goTo('dashboard')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="5 3 19 12 5 21 5 3"/></svg>Launch Inspection</button>
            <button class="btn btn-ghost" onclick="goTo('about')">Learn More</button>
          </div>
        </div>
        <div class="hero-img"><img src="/static/car-nolight.png" alt="Vehicle" onerror="this.src='https://images.unsplash.com/photo-1605559424843-9e4c228bf1c2?w=900&q=80'"><div class="scan-bar"></div></div>
      </div>
      <div class="stats-row">
        <div class="scard"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2" style="margin:0 auto"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg><div class="scard-val">95%</div><div class="scard-lbl">Accuracy Rate</div></div>
        <div class="scard"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2" style="margin:0 auto"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg><div class="scard-val">7</div><div class="scard-lbl">Detection Types</div></div>
        <div class="scard"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2" style="margin:0 auto"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg><div class="scard-val">&lt;2min</div><div class="scard-lbl">Analysis Time</div></div>
        <div class="scard"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2" style="margin:0 auto"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/></svg><div class="scard-val">10K+</div><div class="scard-lbl">Inspections Done</div></div>
      </div>
    </div>
  </section>
  <div class="section">
    <div class="section-hd"><h2>Advanced Testing Suite</h2><p>Powered by computer vision, acoustic AI, and Groq LLaMA 3.3</p></div>
    <div class="feat-grid">
      <div class="fcard"><div class="fcard-ico"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><path d="M19 17h2c.6 0 1-.4 1-1v-3c0-.9-.7-1.7-1.5-1.9C18.7 10.6 16 10 16 10s-1.3-1.4-2.2-2.3c-.5-.4-1.1-.7-1.8-.7H5c-.6 0-1.1.4-1.4.9l-1.4 2.9A3.7 3.7 0 0 0 2 12v4c0 .6.4 1 1 1h2"/><circle cx="7" cy="17" r="2"/><path d="M9 17h6"/><circle cx="17" cy="17" r="2"/></svg></div><h3>AI Body Detection</h3><p>Detects damage across 7 components: bonnet, bumper, dickey, door, fender, lights, windshield with annotated bounding boxes.</p></div>
      <div class="fcard"><div class="fcard-ico"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg></div><h3>Live Camera (iVCam)</h3><p>Real-time webcam detection with auto-capture. Supports iVCam for phone-as-camera with camera source selection.</p></div>
      <div class="fcard"><div class="fcard-ico"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/></svg></div><h3>Engine Knock Detection</h3><p>Upload or record engine audio live. AST model detects pre-detonation knock signatures with full confidence scoring.</p></div>
      <div class="fcard"><div class="fcard-ico"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 8v4l3 3"/></svg></div><h3>AI Health Scoring</h3><p>Groq LLaMA 3.3 analyses all findings to produce a 0–100 vehicle health score, risk level, and personalised recommendations.</p></div>
      <div class="fcard"><div class="fcard-ico"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg></div><h3>Professional PDF Reports</h3><p>Comprehensive A4 reports combining visual, acoustic, and AI findings with annotations, scores, and recommendations.</p></div>
      <div class="fcard"><div class="fcard-ico"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg></div><h3>Instant Results</h3><p>Full inspection pipeline in under 2 minutes. No waiting — just accurate, actionable results.</p></div>
    </div>
  </div>
  <div class="section">
    <div class="section-hd"><h2>How It Works</h2><p>Three steps to a complete AI-powered vehicle diagnostic</p></div>
    <div class="steps-grid">
      <div class="step"><div class="step-num">01</div><h3>Enter Vehicle Data</h3><p>Input VIN, make, model, year and mileage for a personalised inspection report.</p></div>
      <div class="step"><div class="step-num">02</div><h3>Run AI Analysis</h3><p>Upload photos or use live camera for body damage, then upload or record audio for engine knock detection.</p></div>
      <div class="step"><div class="step-num">03</div><h3>Get AI Report</h3><p>Groq LLaMA 3.3 scores your vehicle's health, predicts risk, and generates a detailed PDF with repair recommendations.</p></div>
    </div>
  </div>
  <div class="cta-wrap"><div class="cta-box"><h2>Ready for Next-Gen Inspection?</h2><p>Fast, accurate, and powered by artificial intelligence.</p><div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap"><button class="btn btn-primary" onclick="goTo('dashboard')" style="padding:14px 36px;font-size:1rem">Start Free Inspection Now</button><button class="btn btn-ghost" onclick="goTo('signup')" style="padding:14px 36px;font-size:1rem">Create Account</button></div></div></div>
  <footer><div class="foot-inner"><div class="foot-brand"><span>AutoSpect AI</span><p>Next-generation AI-powered vehicle inspection platform</p></div><div class="foot-col"><h5>Product</h5><a onclick="goTo('dashboard')">Dashboard</a><a onclick="goTo('history')">History</a><a onclick="goTo('about')">About</a></div><div class="foot-col"><h5>Account</h5><a onclick="goTo('signup')">Sign Up</a><a onclick="switchAuthMode('login');goTo('signup')">Sign In</a></div><div class="foot-col"><h5>Legal</h5><a href="#">Privacy Policy</a><a href="#">Terms of Service</a></div></div><div class="foot-bottom">© 2026 AutoSpect AI — All rights reserved.</div></footer>
</div>

<!-- ABOUT -->
<div id="about" class="page">
  <div class="about-wrap">
    <span class="back-link" onclick="goTo('landing')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>Back to Home</span>
    <h1>About AutoSpect AI</h1>
    <p>AutoSpect AI is a next-generation vehicle inspection platform combining computer vision, acoustic signal processing, large language model reasoning, and automated reporting to deliver fast, accurate vehicle diagnostics.</p>
    <p>The audio engine uses <strong style="color:var(--c)">cxlrd/revix-AST-engine-knock</strong> (fine-tuned on the MIT AST base) to detect engine knock from audio recordings. The AI analysis layer uses <strong style="color:var(--c)">Groq LLaMA 3.3 70B</strong> to produce health scores, risk predictions, and personalised recommendations.</p>
    <p>Body damage uses the Roboflow-hosted <strong style="color:var(--c)">Car Damage Detection</strong> model. Reports are generated via ReportLab as professional A4 PDFs.</p>
    <div class="about-feat">
      <div class="af"><h4>Computer Vision</h4><p>Roboflow inference with annotated bounding boxes across 7 damage classes.</p></div>
      <div class="af"><h4>Acoustic AI</h4><p>AST fine-tuned on engine audio for pre-detonation knock detection with confidence scoring.</p></div>
      <div class="af"><h4>Groq LLaMA 3.3</h4><p>Health score (0–100), risk level, AI summary, risk factors, and personalised repair recommendations.</p></div>
      <div class="af"><h4>PDF Reports</h4><p>ReportLab A4 reports with defect tables, annotated images, engine verdict, AI health score, and repair advice.</p></div>
    </div>
    <div style="margin-top:40px;display:flex;gap:12px;flex-wrap:wrap"><button class="btn btn-primary" onclick="goTo('dashboard')">Start Inspection →</button><button class="btn btn-ghost" onclick="goTo('signup')">Create Account</button></div>
  </div>
</div>

<!-- HISTORY -->
<div id="history" class="page">
  <div class="history-wrap">
    <span class="back-link" onclick="goTo('landing')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>Back to Home</span>
    <div class="history-hd"><h1>Inspection History</h1><p>View and manage all your previous vehicle inspections</p></div>
    <div class="history-stats">
      <div class="hstat-card"><div class="hstat-icon blue"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#c8d8e4" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg></div><div><div class="hstat-val" id="hstatTotal">0</div><div class="hstat-lbl">Total Inspections</div></div></div>
      <div class="hstat-card"><div class="hstat-icon green"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg></div><div><div class="hstat-val" id="hstatPassed">0</div><div class="hstat-lbl">Passed</div></div></div>
      <div class="hstat-card"><div class="hstat-icon yellow"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#f59e0b" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg></div><div><div class="hstat-val" id="hstatAttention">0</div><div class="hstat-lbl">Need Attention</div></div></div>
    </div>
    <div class="history-search-row">
      <div class="history-search-wrap"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg><input class="history-search" type="text" placeholder="Search by VIN, vehicle, or date..." id="historySearch" oninput="filterHistory(this.value)"></div>
      <button class="btn btn-outline" onclick="document.getElementById('historySearch').value='';filterHistory('')">All</button>
      <button class="btn-danger-sm" id="btnClearHistory" onclick="clearHistory()" style="display:none"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg>Clear All</button>
    </div>
    <div class="inspection-grid" id="inspectionGrid"></div>
  </div>
</div>

<!-- SIGNUP -->
<div id="signup" class="page">
  <div class="auth-wrap">
    <div class="auth-left">
      <span class="back-link" onclick="goTo('landing')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>Back</span>
      <h1><span class="al1">Join the Future of</span><br><span class="al2">Vehicle Inspection</span></h1>
      <p>Get instant access to AI-powered diagnostics and comprehensive vehicle analysis</p>
      <div class="auth-features">
        <div class="auth-feat-item"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>AI-powered damage detection</div>
        <div class="auth-feat-item"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>Engine audio knock analysis</div>
        <div class="auth-feat-item"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>Groq AI health scoring &amp; risk prediction</div>
        <div class="auth-feat-item"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>Professional PDF reports</div>
      </div>
    </div>
    <div class="auth-card">
      <div class="auth-tabs"><button class="auth-tab" id="loginTab" onclick="switchAuthMode('login')">Sign In</button><button class="auth-tab active" id="signupTab" onclick="switchAuthMode('signup')">Sign Up</button></div>
      <div id="authError" class="auth-error"></div>
      <form onsubmit="handleAuth(event)">
        <div id="nameFields"><div class="auth-form-grid"><div class="auth-form-group"><label>First Name</label><input type="text" id="authFirstName" placeholder="John"></div><div class="auth-form-group"><label>Last Name</label><input type="text" id="authLastName" placeholder="Doe"></div></div></div>
        <div class="auth-form-group"><label>Email</label><input type="email" id="authEmail" placeholder="you@example.com" required></div>
        <div class="auth-form-group"><label>Password</label><input type="password" id="authPassword" placeholder="Create a strong password" required minlength="6"><p class="auth-hint" id="passwordHint">Must be at least 6 characters</p></div>
        <button type="submit" class="btn btn-primary auth-submit" id="authSubmitBtn">Create Account</button>
        <p class="auth-toggle"><span id="authToggleText">Already have an account?</span><a onclick="switchAuthMode(authMode==='signup'?'login':'signup')" id="authToggleLink">Sign In</a></p>
        <div class="auth-divider">By continuing, you agree to our Terms of Service</div>
      </form>
    </div>
  </div>
</div>

<!-- DASHBOARD -->
<div id="dashboard" class="page">
<div class="dash-wrap">
  <span class="back-link" onclick="goTo('landing')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>Back to Home</span>
  <div class="dash-hd"><h1>Inspection Dashboard</h1><p>Complete the steps below to generate a comprehensive AI diagnostic report</p></div>
  <div class="prog-steps">
    <div class="pstep active" id="ps1" onclick="setStep(1)"><div class="ps-num">1</div><span>Vehicle Info</span></div>
    <div class="pstep" id="ps2" onclick="setStep(2)"><div class="ps-num">2</div><span>Body Inspection</span></div>
    <div class="pstep" id="ps3" onclick="setStep(3)"><div class="ps-num">3</div><span>Engine Audio</span></div>
    <div class="pstep" id="ps4" onclick="setStep(4)"><div class="ps-num">4</div><span>Generate Report</span></div>
  </div>

  <!-- STEP 1 -->
  <div id="step1" class="step-content">
    <div class="panel">
      <div class="panel-title"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>Vehicle Information</div>
      <div class="form-grid">
        <div class="form-group"><label>VIN / Registration</label><input type="text" id="vin" placeholder="e.g. 1HGCM82633A004352"></div>
        <div class="form-group"><label>Make</label><input type="text" id="make" placeholder="e.g. Toyota"></div>
        <div class="form-group"><label>Model</label><input type="text" id="model" placeholder="e.g. Camry"></div>
        <div class="form-group"><label>Year</label><input type="text" id="year" placeholder="e.g. 2021"></div>
        <div class="form-group"><label>Mileage</label><input type="text" id="mileage" placeholder="e.g. 45000 km"></div>
      </div>
      <div style="margin-top:24px"><button class="btn btn-primary" onclick="nextStep(2)">Continue to Body Inspection →</button></div>
    </div>
  </div>

  <!-- STEP 2 -->
  <div id="step2" class="step-content" style="display:none">
    <div class="panel">
      <div class="panel-title"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 17h2c.6 0 1-.4 1-1v-3c0-.9-.7-1.7-1.5-1.9C18.7 10.6 16 10 16 10s-1.3-1.4-2.2-2.3c-.5-.4-1.1-.7-1.8-.7H5c-.6 0-1.1.4-1.4.9l-1.4 2.9A3.7 3.7 0 0 0 2 12v4c0 .6.4 1 1 1h2"/><circle cx="7" cy="17" r="2"/><path d="M9 17h6"/><circle cx="17" cy="17" r="2"/></svg>Body Damage Inspection</div>
      <div class="mode-select-grid" style="grid-template-columns:1fr 1fr">
        <div class="mode-select-card active" id="mscUpload" onclick="selectMode('upload')"><div class="msc-icon">📁</div><h4>Upload Images</h4><p>Upload photos for AI batch analysis</p></div>
        <div class="mode-select-card" id="mscLive" onclick="goToLiveCamera()"><div class="msc-icon">📹</div><h4>Live Camera</h4><p>Real-time webcam detection (iVCam)</p></div>
      </div>
      <div id="modeUpload">
        <div class="upload-zone" id="uploadZone" onclick="document.getElementById('fileInput').click()" ondragover="event.preventDefault();this.classList.add('drag')" ondragleave="this.classList.remove('drag')" ondrop="handleDrop(event)">
          <div class="uz-ico"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg></div>
          <p>Drop vehicle images here or click to browse</p><span>JPG · PNG · WEBP — multiple files allowed</span>
        </div>
        <input type="file" id="fileInput" multiple accept="image/*" onchange="handleFiles(this.files)">
        <div id="imgPreviewGrid" class="img-preview-grid"></div>
        <div class="action-row" style="margin-top:20px">
          <button class="btn btn-primary" id="btnAnalyze" onclick="runBatchInspection()" style="display:none"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>Run AI Analysis</button>
        </div>
      </div>
    </div>
    <div id="inlineResults" class="inspection-results-banner">
      <div class="res-header">
        <div class="res-header-top"><div><h2>✅ Inspection Complete!</h2><p>Your vehicle has been analyzed successfully</p></div><div class="res-check-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg></div></div>
        <div class="res-stats">
          <div class="res-stat-card"><div class="res-stat-val" id="resImgCount">—</div><div class="res-stat-lbl">Images Analyzed</div></div>
          <div class="res-stat-card"><div class="res-stat-val" id="resDefectCount">—</div><div class="res-stat-lbl">Defects Detected</div></div>
          <div class="res-stat-card"><div class="res-stat-val" id="resConfidence">—</div><div class="res-stat-lbl">Avg Confidence</div></div>
        </div>
      </div>
      <div class="panel" style="margin-bottom:16px">
        <div style="font-size:.875rem;font-weight:700;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:14px;display:flex;align-items:center;gap:8px"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>Annotated Images <span style="font-size:.72rem;color:var(--td);font-weight:400">(click ✕ to exclude from report)</span></div>
        <div id="resGalleryGrid" class="res-gallery-grid"></div>
      </div>
      <div class="panel" style="margin-bottom:16px">
        <div style="font-size:.875rem;font-weight:700;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:12px">Detected Components</div>
        <div id="resDefectGrid" class="res-defect-grid"></div>
      </div>
      <div class="res-cta-banner">
        <div class="res-cta-text"><h4>Body inspection done — what's next?</h4><p>Add engine audio analysis or go straight to generating your PDF report.</p></div>
        <div class="res-cta-actions">
          <button class="btn btn-ghost" onclick="startNewInspection()"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-3.5"/></svg>New Inspection</button>
          <button class="btn btn-outline" onclick="nextStep(4)">Skip to Report →</button>
          <button class="btn btn-success" onclick="nextStep(3)">Continue to Engine Audio <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></button>
        </div>
      </div>
    </div>
    <div class="action-row"><button class="btn btn-ghost" onclick="nextStep(1)">← Back</button></div>
  </div>

  <!-- STEP 3 -->
  <div id="step3" class="step-content" style="display:none">
    <div class="panel" id="audioUploadCard">
      <div class="panel-title"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/></svg>Engine Audio Analysis <span style="font-size:.75rem;color:var(--tm);font-weight:400;font-family:'JetBrains Mono',monospace;margin-left:8px">(Optional)</span></div>
      <p style="color:var(--tm);font-size:.9rem;margin-bottom:20px">Upload or record engine audio. Our AI will detect pre-detonation knock signatures with confidence scoring.</p>
      <div class="audio-tabs">
        <button class="audio-tab active" id="tabUploadAudio" onclick="switchAudioMode('upload')">📁 Upload File</button>
        <button class="audio-tab" id="tabRecordAudio" onclick="switchAudioMode('record')">🎙 Record Live</button>
      </div>
      <div id="audioModeUpload">
        <div class="audio-drop-zone" id="audioDropZone" onclick="document.getElementById('audioInput').click()" ondragover="event.preventDefault();this.classList.add('drag')" ondragleave="this.classList.remove('drag')" ondrop="handleAudioDrop(event)">
          <div class="adz-icon"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" y1="19" x2="12" y2="23"/><line x1="8" y1="23" x2="16" y2="23"/></svg></div>
          <div class="adz-text"><p>Drop engine audio here or <span style="color:var(--c);font-weight:700">click to browse</span></p><span>WAV · MP3 · M4A · FLAC · OGG</span></div>
          <div class="adz-bars"><div class="adz-bar" style="height:18%"></div><div class="adz-bar" style="height:40%"></div><div class="adz-bar" style="height:65%"></div><div class="adz-bar" style="height:30%"></div><div class="adz-bar" style="height:80%"></div><div class="adz-bar" style="height:55%"></div><div class="adz-bar" style="height:90%"></div><div class="adz-bar" style="height:45%"></div><div class="adz-bar" style="height:70%"></div><div class="adz-bar" style="height:35%"></div><div class="adz-bar" style="height:85%"></div><div class="adz-bar" style="height:50%"></div></div>
        </div>
        <input type="file" id="audioInput" accept="audio/*" onchange="handleAudioFile(this)">
        <div id="audioFileCard" style="display:none;margin-top:16px">
          <div class="audio-file-card">
            <div class="afc-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--c)" stroke-width="2"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/></svg></div>
            <div class="afc-info"><div id="audioFileNameText" class="afc-name"></div><div id="audioFileMeta" class="afc-meta"></div></div>
            <button class="afc-remove" onclick="removeAudioFile()" title="Remove">✕</button>
          </div>
          <div class="audio-player-wrap"><div class="apc-label"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="5 3 19 12 5 21 5 3"/></svg>Preview Recording</div><audio id="audioPlayerEl" controls preload="metadata" style="width:100%;margin-top:4px;border-radius:8px;accent-color:var(--c)"></audio></div>
          <div class="action-row" style="margin-top:16px"><button class="btn btn-primary" id="btnAnalyzeAudio" onclick="runEngineAnalysis()"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>Analyze Engine Audio</button></div>
        </div>
      </div>
      <div id="audioModeRecord" style="display:none">
        <div class="live-rec-zone" id="liveRecZone">
          <div id="recIdleState"><div style="font-size:3rem;margin-bottom:12px">🎙</div><p style="color:var(--t);font-weight:700;font-size:1rem;margin-bottom:6px">Record Engine Audio Live</p><p style="color:var(--tm);font-size:.875rem;margin-bottom:20px">Start recording and rev your engine near the microphone for best results.</p><button class="btn btn-primary" onclick="startRecording()"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="3" fill="currentColor"/></svg>Start Recording</button></div>
          <div id="recActiveState" style="display:none">
            <div style="display:flex;align-items:center;justify-content:center;gap:8px;margin-bottom:8px"><span class="rec-status-dot"></span><span style="font-size:.85rem;font-weight:700;color:var(--red);font-family:'JetBrains Mono',monospace">RECORDING</span></div>
            <div class="rec-timer" id="recTimer">00:00</div>
            <div class="rec-waves" id="recWaves"></div>
            <div class="rec-meter"><div class="rec-meter-fill" id="recMeterFill"></div></div>
            <div style="display:flex;gap:12px;justify-content:center;margin-top:16px"><button class="btn-danger-live" onclick="stopRecording()">⏹ Stop Recording</button><button class="btn btn-ghost" onclick="cancelRecording()">✕ Cancel</button></div>
          </div>
        </div>
        <div id="recordedFileCard" style="display:none;margin-top:16px">
          <div class="audio-file-card">
            <div class="afc-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--green)" stroke-width="2"><path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/></svg></div>
            <div class="afc-info"><div class="afc-name" id="recordedFileName">engine_recording.wav</div><div class="afc-meta" id="recordedFileMeta"></div></div>
            <button class="afc-remove" onclick="clearRecording()" title="Discard">✕</button>
          </div>
          <div class="audio-player-wrap"><div class="apc-label"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="5 3 19 12 5 21 5 3"/></svg>Preview Recording</div><audio id="recordedPlayerEl" controls preload="metadata" style="width:100%;margin-top:4px;border-radius:8px;accent-color:var(--c)"></audio></div>
          <div class="action-row" style="margin-top:16px"><button class="btn btn-primary" id="btnAnalyzeRecording" onclick="analyzeRecordedAudio()"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>Analyze Recording</button><button class="btn btn-ghost" onclick="clearRecording()">Record Again</button></div>
        </div>
      </div>
    </div>
    <div id="audioAnalysisLoader" style="display:none">
      <div class="audio-loader-card">
        <div class="alc-header"><div class="alc-icon-ring"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="var(--c)" stroke-width="1.8"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/></svg></div><div><div class="alc-title">Analyzing Engine Audio</div><div class="alc-sub" id="alc-file-name">Processing...</div></div></div>
        <div class="alc-wave-wrap"><canvas id="alcWaveCanvas" class="alc-wave-canvas"></canvas><div class="alc-scan-line"></div></div>
        <div class="alc-steps">
          <div class="alc-step" id="alcStep0"><div class="alc-step-dot"></div><span>Loading audio data</span></div>
          <div class="alc-step" id="alcStep1"><div class="alc-step-dot"></div><span>Extracting frequency features</span></div>
          <div class="alc-step" id="alcStep2"><div class="alc-step-dot"></div><span>Running AST model inference</span></div>
          <div class="alc-step" id="alcStep3"><div class="alc-step-dot"></div><span>Scoring knock probability</span></div>
          <div class="alc-step" id="alcStep4"><div class="alc-step-dot"></div><span>Generating verdict</span></div>
        </div>
        <div class="alc-progress-wrap"><div class="alc-progress-track"><div class="alc-progress-fill" id="alcProgressFill"></div></div><div class="alc-progress-pct" id="alcProgressPct">0%</div></div>
      </div>
    </div>
    <div id="audioResult" style="display:none"></div>
    <div class="action-row" style="margin-top:4px">
      <button class="btn btn-ghost" onclick="nextStep(2)">← Back to Body Inspection</button>
      <button class="btn btn-primary" onclick="nextStep(4)">Continue to Report →</button>
    </div>
  </div>

  <!-- STEP 4 -->
  <div id="step4" class="step-content" style="display:none">
    <div class="panel">
      <div class="panel-title"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>Generate Inspection Report</div>
      <div id="preSummary" style="margin-bottom:24px"><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:20px" id="summaryCards"></div><p style="color:var(--tm);font-size:.9rem">Review the summary above and choose your PDF option before generating.</p></div>
      <div class="pdf-choice-box"><h4><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>PDF Report Options</h4><div class="pdf-choice-toggle"><button class="pdf-choice-opt" id="pdfOptDownload" onclick="setPdfOption('download')">📥 Download Only</button><button class="pdf-choice-opt active" id="pdfOptPreview" onclick="setPdfOption('preview')">👁 Preview in Browser</button></div></div>
      <div class="action-row">
        <button class="btn btn-ghost" onclick="nextStep(3)">← Back</button>
        <button class="btn btn-primary" id="btnGenerate" onclick="generateReport()"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>Generate PDF Report</button>
        <div id="generateLoading" style="display:none;align-items:center;gap:10px;color:var(--tm);font-size:.875rem"><div class="spinner spinner-light"></div> Generating report + AI analysis...</div>
      </div>
    </div>

    <!-- ✦ AI Analysis Panel — populated after report generation -->
    <div id="aiAnalysisPanel" style="display:none"></div>

    <div id="resultsArea" style="display:none"></div>
    <div id="pdfViewerSection" class="panel" style="display:none;margin-top:24px">
      <div style="font-size:1rem;font-weight:700;margin-bottom:16px;display:flex;align-items:center;gap:10px;color:var(--t)"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--c)" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>📄 Inspection Report</div>
      <iframe id="pdfViewerFrame" title="PDF Report Preview"></iframe>
    </div>
  </div>
</div>
</div>

<!-- LIVE CAMERA -->
<div id="live-camera" class="page">
  <div class="live-wrap">
    <span class="back-link" onclick="returnFromLiveCamera()"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>Back to Inspection</span>
    <div class="live-hd"><h1>📹 Live Camera Inspection</h1><p>Real-time vehicle damage detection — iVCam compatible</p></div>
    <div class="live-camera-card">
      <div class="setup-box"><h5>📱 iVCam Setup</h5><p>1. Install <strong style="color:#fff">iVCam</strong> on your phone (iOS / Android)<br>2. Connect phone and PC to the <strong style="color:#fff">same WiFi</strong> network<br>3. Open iVCam client on your PC — phone becomes a webcam<br>4. Select the iVCam source from the dropdown below<br>5. Click <strong style="color:#fff">Start Camera</strong> to begin live detection</p></div>
      <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:20px">
        <label style="font-size:.8rem;color:var(--tm);font-family:'JetBrains Mono',monospace;font-weight:600;text-transform:uppercase;letter-spacing:.06em;flex-shrink:0">Camera Source:</label>
        <select id="lcCameraSelect" style="background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:8px 14px;color:var(--t);font-family:'Syne',sans-serif;font-size:.85rem;outline:none;cursor:pointer;flex:1;min-width:180px"><option value="">Auto (Default Camera)</option></select>
        <button class="btn btn-ghost" style="padding:8px 16px;font-size:.8rem;flex-shrink:0" onclick="lcRefreshCameras()">🔄 Refresh</button>
      </div>
      <div class="live-stats-row" id="liveStatsRow" style="display:none">
        <div class="lstat"><div class="lstat-val" id="lcStatDetected">0</div><div class="lstat-lbl">Defects Detected</div></div>
        <div class="lstat"><div class="lstat-val" id="lcStatCaptures">0</div><div class="lstat-lbl">Screenshots Captured</div></div>
        <div class="lstat"><div class="lstat-val" id="lcStatStatus">READY</div><div class="lstat-lbl">Camera Status</div></div>
      </div>
      <div id="lcVideoContainer" class="video-wrap" style="display:none">
        <video id="lcVideo" autoplay playsinline style="width:100%;display:block;background:#000;min-height:320px"></video>
        <div class="cam-hud"><div class="hud-row"><span class="live-dot"></span><strong>LIVE</strong></div><div class="hud-row">Detected: <strong id="lcHudDetected">0</strong> defects</div><div class="hud-row">Captures: <strong id="lcHudCaptures">0</strong> screenshots</div></div>
      </div>
      <div class="defect-tags" id="lcDefectTags"></div>
      <div class="cam-controls-row" style="margin-top:20px">
        <button class="btn btn-primary" id="lcBtnStart" onclick="lcStartCamera()"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="5 3 19 12 5 21 5 3"/></svg>Start Camera</button>
        <button class="btn-danger-live" id="lcBtnStop" onclick="lcStopCamera()" style="display:none">⏹ Stop &amp; Finalize</button>
        <button class="btn btn-ghost" onclick="lcReset()">↺ Reset</button>
      </div>
    </div>
    <div id="lcResultsSection" style="display:none">
      <div class="panel">
        <div class="panel-title"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>📸 Captured Annotated Images</div>
        <div id="lcAnnotatedGrid" class="ann-grid"></div>
      </div>
      <div class="panel" style="padding:20px 28px">
        <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:16px">
          <div style="text-align:center"><div style="font-size:1.8rem;font-weight:800;color:var(--c);font-family:'JetBrains Mono',monospace" id="lcSumCaptures">0</div><div style="font-size:.7rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace">Captures</div></div>
          <div style="text-align:center"><div style="font-size:1.8rem;font-weight:800;color:var(--c);font-family:'JetBrains Mono',monospace" id="lcSumDefects">0</div><div style="font-size:.7rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace">Unique Defects</div></div>
          <div style="text-align:center"><div style="font-size:1.8rem;font-weight:800;color:var(--green);font-family:'JetBrains Mono',monospace">✓</div><div style="font-size:.7rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace">Saved to State</div></div>
        </div>
      </div>
      <div style="background:linear-gradient(135deg,rgba(200,216,228,.08),rgba(138,171,184,.06));border:1.5px solid rgba(200,216,228,.25);border-radius:16px;padding:24px 28px;display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap">
        <div><div style="font-size:1rem;font-weight:700;color:#fff;margin-bottom:4px">Live capture complete!</div><div style="font-size:.85rem;color:var(--tm)">Proceed to engine audio or go straight to generating your report.</div></div>
        <div class="action-row">
          <button class="btn btn-ghost" onclick="lcReset()">↺ Capture Again</button>
          <button class="btn btn-outline" onclick="lcGoToReport()">Skip to Report →</button>
          <button class="btn btn-success" onclick="lcGoToAudio()">Continue to Audio <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg></button>
        </div>
      </div>
    </div>
    <div id="lcStatus"></div>
  </div>
</div>

<script>
const API = 'http://localhost:8000';
let authMode = 'signup';
let pdfOption = 'preview';

function freshState() {
  return {
    step: 1, mode: 'upload', files: [],
    batchDone: false, batchDefects: [], batchAnnotated: [],
    perImageDefects: [], crossedIndices: new Set(),
    audioFile: null, audioResult: null, reportReady: false,
    liveCameraResult: null,
    _historySaved: false,
  };
}
let state = freshState();

const HIST_KEYS_KEY = 'history:keys';
let _historyCache = [];

async function saveInspectionToHistory(data) {
  if (state._historySaved) return;
  state._historySaved = true;
  try {
    const make  = (document.getElementById('make').value  || 'Unknown').trim();
    const model = (document.getElementById('model').value || 'Vehicle').trim();
    const vin   = (document.getElementById('vin').value   || '—').trim();
    const year  = (document.getElementById('year').value  || '').trim();
    const defects = data.defects_detected || [];
    const uniqueTypes = data.unique_defect_types != null
      ? data.unique_defect_types
      : new Set(defects.map(d => (Array.isArray(d) ? d[0] : d.label || '').toLowerCase()).filter(Boolean)).size;
    const status = uniqueTypes === 0 ? 'pass' : uniqueTypes <= 2 ? 'attention' : 'fail';
    let image = '';
    if (state.files && state.files.length > 0) {
      try {
        image = await new Promise((res, rej) => {
          const r = new FileReader();
          r.onload = () => res(r.result);
          r.onerror = () => rej(new Error('read failed'));
          r.readAsDataURL(state.files[0]);
        });
      } catch(e) { image = ''; }
    }
    if (!image) {
      const ann = data.annotated_images || [];
      image = ann.length ? `${API}/${ann[0]}` : '';
    }
    const id      = 'insp_' + Date.now();
    const dateStr = new Date().toLocaleDateString('en-US', { year:'numeric', month:'long', day:'numeric' });
    const record  = {
      id, date: dateStr, timestamp: Date.now(),
      vehicle: `${make} ${model}${year ? ' '+year : ''}`.trim(),
      vin, status, defects: uniqueTypes, image,
      engineKnock: data.engine_result ? data.engine_result.is_knock : null
    };
    let keys = [];
    try { keys = JSON.parse(localStorage.getItem(HIST_KEYS_KEY) || '[]'); } catch(e) {}
    keys = [...new Set(keys)];
    keys.unshift(id);
    localStorage.setItem(id, JSON.stringify(record));
    localStorage.setItem(HIST_KEYS_KEY, JSON.stringify(keys));
  } catch(err) {
    state._historySaved = false;
    console.warn('[History] save failed:', err);
  }
}

async function loadHistory() {
  try {
    let keys = [];
    try { keys = JSON.parse(localStorage.getItem(HIST_KEYS_KEY) || '[]'); } catch(e) {}
    const unique = [...new Set(keys)];
    if (unique.length !== keys.length) localStorage.setItem(HIST_KEYS_KEY, JSON.stringify(unique));
    const records = []; const seen = new Set();
    for (const k of unique) {
      if (seen.has(k)) continue; seen.add(k);
      try { const raw = localStorage.getItem(k); if (raw) records.push(JSON.parse(raw)); } catch(e) {}
    }
    return records;
  } catch(e) { return []; }
}

async function clearHistory() {
  if (!confirm('Delete all inspection history?')) return;
  let keys = []; try { keys = JSON.parse(localStorage.getItem(HIST_KEYS_KEY) || '[]'); } catch(e) {}
  keys.forEach(k => { try { localStorage.removeItem(k); } catch(e) {} });
  localStorage.removeItem(HIST_KEYS_KEY);
  _historyCache = [];
  renderHistory([]); updateHistoryStats([]);
  toast('History cleared', 'info');
  const btn = document.getElementById('btnClearHistory');
  if (btn) btn.style.display = 'none';
}

async function loadAndRenderHistory() {
  const grid = document.getElementById('inspectionGrid');
  grid.innerHTML = `<div style="grid-column:1/-1;text-align:center;padding:40px;color:var(--tm);font-family:'JetBrains Mono',monospace;font-size:.85rem"><div class="spinner spinner-light" style="margin:0 auto 12px;width:24px;height:24px"></div>Loading...</div>`;
  const records = await loadHistory();
  _historyCache = records;
  renderHistory(records); updateHistoryStats(records);
  const btn = document.getElementById('btnClearHistory');
  if (btn) btn.style.display = records.length ? 'inline-flex' : 'none';
}

function updateHistoryStats(records) {
  document.getElementById('hstatTotal').textContent     = records.length;
  document.getElementById('hstatPassed').textContent    = records.filter(r => r.status === 'pass').length;
  document.getElementById('hstatAttention').textContent = records.filter(r => r.status !== 'pass').length;
}

function renderHistory(data) {
  const grid = document.getElementById('inspectionGrid'); if (!grid) return;
  if (!data || !data.length) {
    grid.innerHTML = `<div class="history-empty"><div class="history-empty-icon">🔍</div><h3>No Inspections Yet</h3><p>Your completed vehicle inspections will appear here.</p><button class="btn btn-primary" onclick="goTo('dashboard')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="5 3 19 12 5 21 5 3"/></svg>Start First Inspection</button></div>`;
    return;
  }
  const sLabel = { pass:'PASS', attention:'ATTENTION', fail:'FAIL' };
  const sIcon  = {
    pass:      `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>`,
    attention: `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/></svg>`,
    fail:      `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg>`
  };
  const fb = {
    pass:      'https://images.unsplash.com/photo-1560958089-b8a1929cea89?w=400',
    attention: 'https://images.unsplash.com/photo-1555215695-3004980ad54e?w=400',
    fail:      'https://images.unsplash.com/photo-1605559424843-9e4c228bf1c2?w=400'
  };
  grid.innerHTML = data.map(item => `
    <div class="insp-card">
      <div class="insp-card-img"><img src="${item.image||fb[item.status]}" alt="${item.vehicle}" loading="lazy" onerror="this.src='${fb[item.status]}'"><div class="insp-card-img-overlay"></div><div class="insp-status-badge ${item.status}">${sIcon[item.status]} ${sLabel[item.status]}</div></div>
      <div class="insp-content">
        <div class="insp-vehicle">${item.vehicle}</div><div class="insp-vin">VIN: ${item.vin||'—'}</div>
        <div class="insp-meta"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>${item.date}</div>
        <div class="insp-nums">
          <div class="insp-num-box"><div class="insp-num">${item.defects}</div><div class="insp-num-lbl">Defects Found</div></div>
          <div class="insp-num-box"><div class="insp-num" style="color:${item.engineKnock===true?'var(--red)':item.engineKnock===false?'var(--green)':'var(--td)'}">${item.engineKnock===true?'⚠':item.engineKnock===false?'✓':'—'}</div><div class="insp-num-lbl">Engine Audio</div></div>
        </div>
        <div><button onclick="goTo('dashboard')" class="btn btn-primary" style="width:100%;justify-content:center;padding:8px 12px;font-size:.8rem">New Inspection</button></div>
      </div>
    </div>`).join('');
}

function filterHistory(q) {
  if (!q.trim()) { renderHistory(_historyCache); return; }
  const lq = q.toLowerCase();
  renderHistory(_historyCache.filter(i =>
    (i.vehicle||'').toLowerCase().includes(lq) ||
    (i.vin||'').toLowerCase().includes(lq) ||
    (i.date||'').toLowerCase().includes(lq)
  ));
}

window._handleAuthChange = function(user) {
  const ni = document.getElementById('navUserInfo');
  const nb = document.getElementById('navAuthBtn');
  if (user) {
    document.getElementById('navUserEmail').textContent = user.email;
    ni.style.display = 'flex'; nb.style.display = 'none';
  } else {
    ni.style.display = 'none'; nb.style.display = 'inline-block';
  }
};

async function handleAuth(e) {
  e.preventDefault();
  const email = document.getElementById('authEmail').value.trim();
  const pw    = document.getElementById('authPassword').value;
  const errEl = document.getElementById('authError');
  const btn   = document.getElementById('authSubmitBtn');
  errEl.classList.remove('show');
  btn.disabled  = true;
  btn.innerHTML = '<div class="spinner"></div>';
  try {
    if (!window._fbAuth) throw new Error('Firebase not initialised.');
    if (authMode === 'signup') { await window._fbCreateUser(window._fbAuth, email, pw); toast('✓ Account created!', 'success'); }
    else { await window._fbSignIn(window._fbAuth, email, pw); toast('✓ Signed in!', 'success'); }
    setTimeout(() => goTo('dashboard'), 800);
  } catch(err) {
    let msg = err.message || 'Authentication failed';
    if (msg.includes('email-already-in-use'))                          msg = 'Email already in use.';
    else if (msg.includes('invalid-credential')||msg.includes('wrong-password')) msg = 'Invalid email or password.';
    else if (msg.includes('weak-password'))                            msg = 'Password must be at least 6 characters.';
    errEl.textContent = msg; errEl.classList.add('show');
  }
  btn.disabled = false;
  btn.textContent = authMode === 'signup' ? 'Create Account' : 'Sign In';
}

async function signOutUser() {
  if (window._fbAuth && window._fbSignOut) {
    await window._fbSignOut(window._fbAuth);
    toast('Signed out', 'info'); goTo('landing');
  }
}

function switchAuthMode(mode) {
  authMode = mode; const s = mode === 'signup';
  document.getElementById('loginTab').classList.toggle('active', !s);
  document.getElementById('signupTab').classList.toggle('active', s);
  document.getElementById('nameFields').style.display  = s ? '' : 'none';
  document.getElementById('passwordHint').style.display = s ? '' : 'none';
  const btn = document.getElementById('authSubmitBtn');
  btn.textContent = s ? 'Create Account' : 'Sign In';
  document.getElementById('authToggleText').textContent = s ? 'Already have an account?' : "Don't have an account?";
  document.getElementById('authToggleLink').textContent = s ? 'Sign In' : 'Sign Up';
  document.getElementById('authError').classList.remove('show');
}

function goTo(page) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById(page).classList.add('active');
  window.scrollTo(0, 0);
  if (page === 'history') loadAndRenderHistory();
}
function goToLiveCamera() {
  lcRefreshCameras();
  goTo('live-camera');
  document.getElementById('mscLive').classList.add('active');
  document.getElementById('mscUpload').classList.remove('active');
}
function returnFromLiveCamera() { lcStopStreamOnly(); goTo('dashboard'); setStep(2); }

let toastTimer;
function toast(msg, type='info') {
  const el = document.getElementById('toast');
  el.textContent = msg; el.className = `show ${type}`;
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => el.classList.remove('show'), 3200);
}

function setStep(n) {
  for (let i = 1; i <= 4; i++) {
    const c = document.getElementById('step'+i);
    const p = document.getElementById('ps'+i);
    if (c) c.style.display = i === n ? 'block' : 'none';
    if (p) {
      p.classList.remove('active','done');
      if (i < n) p.classList.add('done');
      else if (i === n) p.classList.add('active');
    }
  }
  state.step = n;
  if (n === 4) buildSummaryCards();
}
function nextStep(n) {
  setStep(n);
  window.scrollTo({ top: document.getElementById('dashboard').offsetTop, behavior: 'smooth' });
}
function selectMode(m) {
  state.mode = m;
  document.getElementById('mscUpload').classList.toggle('active', m === 'upload');
}
function setPdfOption(o) {
  pdfOption = o;
  document.getElementById('pdfOptDownload').classList.toggle('active', o === 'download');
  document.getElementById('pdfOptPreview').classList.toggle('active', o === 'preview');
}

function handleFiles(files) {
  for (const f of files) {
    if (f.type.startsWith('image/') && !state.files.find(x => x.name === f.name && x.size === f.size))
      state.files.push(f);
  }
  renderPreviews();
}
function handleDrop(e) {
  e.preventDefault();
  document.getElementById('uploadZone').classList.remove('drag');
  handleFiles(e.dataTransfer.files);
}
function renderPreviews() {
  const grid = document.getElementById('imgPreviewGrid'); grid.innerHTML = '';
  state.files.forEach((f,i) => {
    const url = URL.createObjectURL(f);
    const div = document.createElement('div'); div.className = 'img-thumb';
    div.innerHTML = `<img src="${url}"><button class="img-thumb-rm" onclick="removeFile(${i})">✕</button>`;
    grid.appendChild(div);
  });
  document.getElementById('btnAnalyze').style.display = state.files.length ? 'inline-flex' : 'none';
}
function removeFile(i) { state.files.splice(i,1); renderPreviews(); }

let _scanTimer = null;
function showScanOverlay(files) {
  const strip = document.getElementById('scanImgStrip'); strip.innerHTML = '';
  const show  = Array.from(files).slice(0,5);
  show.forEach((f,i) => {
    const url  = URL.createObjectURL(f);
    const slot = document.createElement('div');
    slot.className = 'scan-img-slot'; slot.id = `scanSlot${i}`;
    slot.innerHTML = `<img src="${url}"><div class="scan-corner tl"></div><div class="scan-corner tr"></div><div class="scan-corner bl"></div><div class="scan-corner br"></div><div class="scan-line"></div><div class="done-tick">✓</div>`;
    strip.appendChild(slot);
  });
  if (files.length > 5) {
    const m = document.createElement('div');
    m.style.cssText = 'display:flex;align-items:center;justify-content:center;color:var(--tm);font-family:"JetBrains Mono",monospace;font-size:.8rem;padding:0 8px';
    m.textContent = `+${files.length-5} more`; strip.appendChild(m);
  }
  setScanProgress(0,'Preparing images...');
  document.getElementById('scanOverlay').classList.add('active');
  let pi=0, ii=0; const tsl=show.length;
  const phases=[[8,'Loading image data...'],[20,'Running visual preprocessing...'],[35,'Detecting vehicle components...'],[52,'Analyzing damage patterns...'],[66,'Running confidence scoring...'],[78,'Cross-referencing defect library...'],[88,'Generating annotations...'],[94,'Compiling results...'],[98,'Finalizing...']];
  _scanTimer = setInterval(() => {
    if (pi < phases.length) {
      const [pct,msg] = phases[pi];
      setScanProgress(pct,msg);
      const sp = Math.floor((pi/phases.length)*tsl);
      if (sp !== ii && sp < tsl) {
        document.getElementById(`scanSlot${ii}`)?.classList.remove('active-scan');
        document.getElementById(`scanSlot${ii}`)?.classList.add('done-scan');
        ii = sp;
        document.getElementById(`scanSlot${ii}`)?.classList.add('active-scan');
      }
      pi++;
    }
  }, 420);
  if (show.length) document.getElementById('scanSlot0')?.classList.add('active-scan');
}
function setScanProgress(pct,msg) {
  document.getElementById('scanProgressFill').style.width = pct+'%';
  document.getElementById('scanProgressPct').textContent  = pct+'%';
  if (msg) document.getElementById('scanStatusMsg').textContent = msg;
}
function hideScanOverlay() {
  clearInterval(_scanTimer);
  document.querySelectorAll('.scan-img-slot').forEach(s => { s.classList.remove('active-scan'); s.classList.add('done-scan'); });
  setScanProgress(100,'Analysis complete ✓');
  setTimeout(() => document.getElementById('scanOverlay').classList.remove('active'), 700);
}

let _batchRunning = false;
async function runBatchInspection() {
  if (_batchRunning) return;
  if (!state.files.length) return toast('Please add at least one image','error');
  _batchRunning = true;
  const btn = document.getElementById('btnAnalyze');
  btn.disabled = true; btn.style.display = 'none';
  state._historySaved = false;
  state.liveCameraResult = null;
  showScanOverlay(state.files);
  document.getElementById('inlineResults').classList.remove('visible');
  const fd = new FormData();
  state.files.forEach(f => fd.append('files',f));
  appendVehicleInfo(fd);
  try {
    const res = await fetch(`${API}/inspect`,{method:'POST',body:fd});
    if (!res.ok) throw new Error((await res.json()).detail || 'Inspection failed');
    const data = await res.json();
    state.batchDefects  = data.defects_detected  || [];
    state.batchAnnotated= data.annotated_images  || [];
    state.batchDone     = true;
    state.crossedIndices= new Set();
    const annotated   = data.annotated_images || [];
    const allDef      = data.defects_detected || [];
    const totalFiles  = state.files.length;
    state.perImageDefects = [];
    let di = 0;
    const pic = annotated.length > 0 ? Math.ceil(allDef.length / annotated.length) : 0;
    for (let i=0; i<totalFiles; i++) {
      const mp = annotated.find(p => p.includes(`_${i}.`)) || null;
      if (mp) { state.perImageDefects.push({defects:allDef.slice(di,di+pic),annotated:mp}); di+=pic; }
      else      state.perImageDefects.push({defects:[],annotated:null});
    }
    await saveInspectionToHistory(data);
    hideScanOverlay();
    populateInlineResults(data);
    toast(`✓ Analysis complete — ${data.total_defects_detected||state.batchDefects.length} defects found`,'success');
  } catch(e) {
    hideScanOverlay();
    toast(e.message,'error');
  }
  _batchRunning = false;
  btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg> Re-Analyze';
  btn.disabled = false; btn.style.display = 'inline-flex';
}

function populateInlineResults(data) {
  const annotated = data.annotated_images || [];
  const imgCount  = data.image_count || state.files.length;
  const ts        = Date.now();
  const gallery   = document.getElementById('resGalleryGrid'); gallery.innerHTML = '';
  const crossSVG  = `<svg viewBox="0 0 100 100" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg"><line x1="4" y1="4" x2="96" y2="96" stroke="rgba(255,68,68,0.85)" stroke-width="3" stroke-linecap="round"/><line x1="96" y1="4" x2="4" y2="96" stroke="rgba(255,68,68,0.85)" stroke-width="3" stroke-linecap="round"/></svg>`;
  const count = annotated.length || imgCount;
  for (let i=0; i<count; i++) {
    const item = document.createElement('div');
    item.className = 'res-gallery-item'; item.dataset.imgIndex = i;
    if (annotated[i]) {
      item.innerHTML = `<img src="${API}/${annotated[i]}?t=${ts}" alt="Annotated ${i+1}" onerror="this.style.display='none'"><div class="res-cross-overlay">${crossSVG}</div><div class="res-gallery-badge">IMG ${i+1}</div><button class="res-gallery-dismiss" title="Exclude from report" onclick="toggleGalleryItem(this)">✕</button>`;
    } else {
      item.innerHTML = `<div class="res-gallery-placeholder"><span>🚗</span><p>Image ${i+1}</p></div><div class="res-cross-overlay">${crossSVG}</div><button class="res-gallery-dismiss" onclick="toggleGalleryItem(this)">✕</button>`;
    }
    gallery.appendChild(item);
  }
  refreshInlineStats();
  document.getElementById('inlineResults').classList.add('visible');
  setTimeout(() => document.getElementById('inlineResults').scrollIntoView({behavior:'smooth',block:'start'}), 100);
}

function refreshInlineStats() {
  let active = [];
  if (state.perImageDefects.length) {
    state.perImageDefects.forEach((img,i) => { if (!state.crossedIndices.has(i)) active.push(...img.defects); });
  } else {
    active = [...state.batchDefects];
  }
  const totalImgs = state.perImageDefects.length || state.batchAnnotated.length || state.files.length;
  document.getElementById('resImgCount').textContent = totalImgs - state.crossedIndices.size;
  const grouped = {};
  active.forEach(d => {
    const name = Array.isArray(d) ? (d[0]||'Unknown') : (d.label||d.class||'Unknown');
    const conf = Array.isArray(d) ? (d[1]||0)         : (d.confidence||0);
    const k    = name.toLowerCase();
    if (!grouped[k] || grouped[k].conf < conf) grouped[k] = {name, conf};
  });
  const sorted = Object.values(grouped).sort((a,b) => b.conf-a.conf);
  const defEl = document.getElementById('resDefectCount');
  defEl.textContent = sorted.length;
  defEl.className   = 'res-stat-val '+(sorted.length===0?'green':sorted.length<=2?'yellow':'red');
  const avg  = active.length ? Math.round(active.reduce((s,d)=>s+(Array.isArray(d)?d[1]:d.confidence||0),0)/active.length) : 0;
  const cEl  = document.getElementById('resConfidence');
  cEl.textContent = avg+'%';
  cEl.className   = 'res-stat-val '+(avg>=80?'green':avg>=60?'yellow':'red');
  const dg = document.getElementById('resDefectGrid'); dg.innerHTML = '';
  if (!sorted.length) {
    dg.innerHTML = `<div class="res-no-defects">✅ No damage detected — vehicle looks clean!</div>`;
  } else {
    sorted.forEach(({name,conf}) => {
      const row = document.createElement('div'); row.className = 'res-defect-row';
      row.innerHTML = `<div class="res-defect-name">${name}</div><div class="res-defect-bar-wrap"><div class="res-defect-bar" style="width:0%"></div></div><div class="res-defect-conf">${conf}%</div>`;
      dg.appendChild(row);
      requestAnimationFrame(() => requestAnimationFrame(() => { row.querySelector('.res-defect-bar').style.width = conf+'%'; }));
    });
  }
}

function toggleGalleryItem(btn) {
  const item    = btn.closest('.res-gallery-item');
  const idx     = parseInt(item.dataset.imgIndex, 10);
  const crossed = item.classList.toggle('crossed');
  if (crossed) state.crossedIndices.add(idx); else state.crossedIndices.delete(idx);
  btn.title     = crossed ? 'Restore' : 'Exclude from report';
  btn.textContent = crossed ? '↩' : '✕';
  refreshInlineStats();
  toast(crossed ? `Image ${idx+1} excluded from report` : `Image ${idx+1} restored`, 'info');
}

let lcStream=null, lcInterval=null, lcIsProcessing=false;
let lcCaptures=0, lcDetected=0, lcDefectSet=new Set();
let lcAllDefectsCollected=[];
let _lcStopInProgress=false;

async function lcRefreshCameras() {
  const sel = document.getElementById('lcCameraSelect');
  try {
    const tmp = await navigator.mediaDevices.getUserMedia({video:true,audio:false});
    tmp.getTracks().forEach(t=>t.stop());
    const devices = await navigator.mediaDevices.enumerateDevices();
    const cams    = devices.filter(d => d.kind==='videoinput');
    sel.innerHTML = '<option value="">Auto (Default Camera)</option>';
    cams.forEach((d,i) => {
      const o = document.createElement('option');
      o.value       = d.deviceId;
      o.textContent = d.label || `Camera ${i+1}`;
      if ((d.label||'').toLowerCase().includes('ivcam')) o.textContent = '📱 '+o.textContent;
      sel.appendChild(o);
    });
    toast(`Found ${cams.length} camera(s)`,'info');
  } catch(e) { toast('Camera permission needed','error'); }
}

async function lcStartCamera() {
  try {
    const deviceId    = document.getElementById('lcCameraSelect').value;
    const constraints = { video: deviceId ? {deviceId:{exact:deviceId},width:{ideal:1920},height:{ideal:1080}} : {width:{ideal:1920},height:{ideal:1080}} };
    lcStream = await navigator.mediaDevices.getUserMedia(constraints);
    document.getElementById('lcVideo').srcObject = lcStream;
    document.getElementById('lcVideoContainer').style.display = 'block';
    document.getElementById('liveStatsRow').style.display     = 'grid';
    document.getElementById('lcBtnStart').style.display       = 'none';
    document.getElementById('lcBtnStop').style.display        = 'inline-flex';
    document.getElementById('lcResultsSection').style.display = 'none';
    document.getElementById('lcStatus').innerHTML             = '';
    lcCaptures=0; lcDetected=0; lcDefectSet.clear(); lcAllDefectsCollected=[];
    ['lcHudDetected','lcHudCaptures','lcStatDetected','lcStatCaptures'].forEach(id=>document.getElementById(id).textContent='0');
    document.getElementById('lcStatStatus').textContent = 'LIVE';
    document.getElementById('lcDefectTags').innerHTML   = '';
    _lcStopInProgress = false;
    lcInterval = setInterval(lcSendFrame, 1500);
    const sel = document.getElementById('lcCameraSelect');
    toast(`Camera started: ${sel.options[sel.selectedIndex]?.text||'Default'}`, 'success');
  } catch(err) { lcShowStatus('Camera access denied: '+err.message,'error'); }
}

async function lcSendFrame() {
  if (lcIsProcessing) return;
  lcIsProcessing = true;
  const video  = document.getElementById('lcVideo');
  const canvas = document.createElement('canvas');
  canvas.width  = video.videoWidth  || 640;
  canvas.height = video.videoHeight || 480;
  canvas.getContext('2d').drawImage(video,0,0);
  canvas.toBlob(async (blob) => {
    try {
      const fd  = new FormData(); fd.append('file', blob, 'frame.jpg');
      const res = await fetch(`${API}/detect-live`,{method:'POST',body:fd});
      if (!res.ok) return;
      const data = await res.json();
      lcDetected = data.unique_defects  || 0;
      lcCaptures = data.total_captures  || 0;
      document.getElementById('lcHudDetected').textContent  = lcDetected;
      document.getElementById('lcHudCaptures').textContent  = lcCaptures;
      document.getElementById('lcStatDetected').textContent = lcDetected;
      document.getElementById('lcStatCaptures').textContent = lcCaptures;
      if (data.defects && data.defects.length) {
        data.defects.forEach(d => {
          lcAllDefectsCollected.push([d.class, d.confidence]);
          const k = (d.class||'').toLowerCase();
          if (!lcDefectSet.has(k)) {
            lcDefectSet.add(k);
            const tags = document.getElementById('lcDefectTags');
            const tag  = document.createElement('div'); tag.className = 'dtag new';
            tag.textContent = `${d.class} ${d.confidence}%`;
            tags.appendChild(tag);
            setTimeout(() => tag.classList.remove('new'), 500);
          }
        });
      }
      if (data.new_capture) {
        const con = document.getElementById('lcVideoContainer');
        const ex  = con.querySelector('.capture-badge'); if (ex) ex.remove();
        const b   = document.createElement('div'); b.className = 'capture-badge'; b.textContent = '📸 Captured!';
        con.appendChild(b); setTimeout(() => b.remove(), 900);
      }
    } catch(e) {}
    finally { lcIsProcessing = false; }
  }, 'image/jpeg', 0.9);
}

async function lcStopCamera() {
  if (_lcStopInProgress) return;
  _lcStopInProgress = true;
  const stopBtn  = document.getElementById('lcBtnStop');
  const startBtn = document.getElementById('lcBtnStart');
  stopBtn.disabled = true;
  lcStopStreamOnly();
  document.getElementById('lcStatStatus').textContent = 'STOPPED';
  stopBtn.style.display  = 'none';
  startBtn.style.display = 'inline-flex';
  startBtn.textContent   = '▶ Resume';
  stopBtn.disabled       = false;
  if (lcCaptures > 0) {
    lcShowStatus('<div style="display:flex;align-items:center;gap:10px;justify-content:center"><div class="spinner spinner-light"></div> Finalizing captures...</div>','info');
    try {
      const res  = await fetch(`${API}/get-captured-images`);
      const data = await res.json();
      const paths = (data.images||[]).map(i => i.path);
      const defectMap = {};
      lcAllDefectsCollected.forEach(([name,conf]) => {
        const k = (name||'').toLowerCase();
        if (!defectMap[k] || defectMap[k][1] < conf) defectMap[k] = [name, conf];
      });
      const deduped = Object.values(defectMap);
      state.batchAnnotated    = paths;
      state.batchDefects      = deduped;
      state.batchDone         = true;
      state.perImageDefects   = [];
      state.liveCameraResult  = { annotated_images:paths, defects_detected:deduped, unique_defect_types:lcDetected };
      state._historySaved = false;
      await saveInspectionToHistory({
        defects_detected:  deduped,
        annotated_images:  paths,
        unique_defect_types: lcDetected,
        engine_result:     null
      });
      lcDisplayResults(paths);
    } catch(e) {
      lcShowStatus('Error fetching captures: '+e.message,'error');
      _lcStopInProgress = false;
    }
  } else {
    lcShowStatus('No defects detected. Try capturing the vehicle from different angles.','error');
    try { await fetch(`${API}/reset-live-detection`,{method:'POST'}); } catch(e) {}
    _lcStopInProgress = false;
  }
}

function lcStopStreamOnly() {
  if (lcInterval) { clearInterval(lcInterval); lcInterval = null; }
  if (lcStream)   { lcStream.getTracks().forEach(t=>t.stop()); lcStream = null; }
}

function lcDisplayResults(paths) {
  lcShowStatus('','');
  const grid = document.getElementById('lcAnnotatedGrid'); const ts = Date.now();
  grid.innerHTML = paths.map((p,i) =>
    `<div class="ann-img"><img src="${API}/${p}?t=${ts}" alt="Capture ${i+1}" onerror="this.alt='Image unavailable'"><div class="ann-img-label">Capture ${i+1}</div></div>`
  ).join('');
  document.getElementById('lcSumCaptures').textContent = paths.length;
  document.getElementById('lcSumDefects').textContent  = lcDetected;
  document.getElementById('lcResultsSection').style.display = 'block';
  document.getElementById('lcResultsSection').scrollIntoView({behavior:'smooth'});
  toast(`✅ ${paths.length} annotated image(s) captured`, 'success');
}

function lcGoToAudio()  { goTo('dashboard'); setStep(3); }
function lcGoToReport() { goTo('dashboard'); setStep(4); }

function lcReset() {
  lcStopStreamOnly();
  lcCaptures=0; lcDetected=0; lcDefectSet.clear(); lcAllDefectsCollected=[];
  _lcStopInProgress = false;
  document.getElementById('lcVideoContainer').style.display  = 'none';
  document.getElementById('liveStatsRow').style.display      = 'none';
  document.getElementById('lcResultsSection').style.display  = 'none';
  document.getElementById('lcBtnStart').style.display        = 'inline-flex';
  document.getElementById('lcBtnStart').textContent          = '▶ Start Camera';
  document.getElementById('lcBtnStop').style.display         = 'none';
  document.getElementById('lcBtnStop').disabled              = false;
  document.getElementById('lcDefectTags').innerHTML          = '';
  document.getElementById('lcStatus').innerHTML              = '';
  try { fetch(`${API}/reset-live-detection`,{method:'POST'}); } catch(e) {}
  toast('Live detection reset','info');
}

function lcShowStatus(msg,type) {
  const el = document.getElementById('lcStatus');
  if (!msg) { el.innerHTML=''; return; }
  const cm = {info:'rgba(200,216,228,.1)',error:'rgba(255,68,68,.08)',success:'rgba(74,222,128,.08)'};
  const bm = {info:'rgba(200,216,228,.3)',error:'rgba(255,68,68,.3)',success:'rgba(74,222,128,.3)'};
  el.innerHTML = `<div style="margin-top:16px;padding:16px 20px;border-radius:12px;background:${cm[type]||cm.info};border:1px solid ${bm[type]||bm.info};font-size:.875rem;text-align:center">${msg}</div>`;
}

function switchAudioMode(mode) {
  document.getElementById('tabUploadAudio').classList.toggle('active', mode==='upload');
  document.getElementById('tabRecordAudio').classList.toggle('active', mode==='record');
  document.getElementById('audioModeUpload').style.display = mode==='upload' ? 'block' : 'none';
  document.getElementById('audioModeRecord').style.display = mode==='record' ? 'block' : 'none';
}

function handleAudioDrop(e) {
  e.preventDefault();
  document.getElementById('audioDropZone').classList.remove('drag');
  const f = e.dataTransfer.files[0];
  if (f && f.type.startsWith('audio/')) { state.audioFile = f; showAudioFileCard(f); }
  else toast('Please drop an audio file','error');
}
function handleAudioFile(input) {
  const f = input.files[0]; if (!f) return;
  state.audioFile = f; showAudioFileCard(f);
}
function showAudioFileCard(f) {
  document.getElementById('audioFileNameText').textContent = f.name;
  document.getElementById('audioFileMeta').textContent     = `${(f.size/1024).toFixed(1)} KB · ${f.type||'audio'}`;
  document.getElementById('audioDropZone').style.display   = 'none';
  document.getElementById('audioFileCard').style.display   = 'block';
  const player = document.getElementById('audioPlayerEl');
  player.src = URL.createObjectURL(f); player.load();
}
function removeAudioFile() {
  state.audioFile = null;
  document.getElementById('audioInput').value            = '';
  document.getElementById('audioDropZone').style.display = 'block';
  document.getElementById('audioFileCard').style.display = 'none';
  document.getElementById('audioResult').style.display   = 'none';
  const p = document.getElementById('audioPlayerEl'); p.pause(); p.src='';
}

function resetAudioInput() {
  state.audioFile = null;
  document.getElementById('audioResult').style.display = 'none';
  const mode = document.getElementById('audioModeRecord').style.display === 'block' ? 'record' : 'upload';
  if (mode === 'record') {
    clearRecording();
  } else {
    document.getElementById('audioInput').value            = '';
    document.getElementById('audioDropZone').style.display = 'block';
    document.getElementById('audioFileCard').style.display = 'none';
    const p = document.getElementById('audioPlayerEl'); p.pause(); p.src='';
  }
}

let _mediaRecorder=null, _recordedChunks=[], _recTimerInterval=null, _recSeconds=0;
let _audioContext=null, _analyserNode=null, _micStream=null, _waveAnimId=null;
let _recordingCancelled=false;

async function startRecording() {
  try {
    _micStream = await navigator.mediaDevices.getUserMedia({audio:true,video:false});
  } catch(e) { toast('Microphone access denied','error'); return; }
  _recordedChunks = []; _recordingCancelled = false;
  const mimeType = ['audio/webm;codecs=opus','audio/webm','audio/ogg;codecs=opus','audio/mp4']
    .find(m => MediaRecorder.isTypeSupported(m)) || '';
  _mediaRecorder = new MediaRecorder(_micStream, mimeType ? {mimeType} : {});
  _mediaRecorder.ondataavailable = e => { if (e.data && e.data.size>0) _recordedChunks.push(e.data); };
  _mediaRecorder.onstop = () => { if (!_recordingCancelled) finalizeRecording(); };
  _mediaRecorder.start(100);
  document.getElementById('recIdleState').style.display  = 'none';
  document.getElementById('recActiveState').style.display= 'block';
  document.getElementById('liveRecZone').classList.add('recording');
  _recSeconds = 0; document.getElementById('recTimer').textContent = '00:00';
  _recTimerInterval = setInterval(() => {
    _recSeconds++;
    const m = Math.floor(_recSeconds/60).toString().padStart(2,'0');
    const s = (_recSeconds%60).toString().padStart(2,'0');
    document.getElementById('recTimer').textContent = `${m}:${s}`;
  }, 1000);
  const wavesEl = document.getElementById('recWaves'); wavesEl.innerHTML = '';
  const bars = []; const barCount = 20;
  for (let i=0; i<barCount; i++) {
    const b = document.createElement('div'); b.className='rec-wave-bar'; b.style.height='4px';
    wavesEl.appendChild(b); bars.push(b);
  }
  _audioContext = new (window.AudioContext||window.webkitAudioContext)();
  _analyserNode = _audioContext.createAnalyser(); _analyserNode.fftSize = 64;
  const src = _audioContext.createMediaStreamSource(_micStream); src.connect(_analyserNode);
  const buf = new Uint8Array(_analyserNode.frequencyBinCount);
  function animWave() {
    _waveAnimId = requestAnimationFrame(animWave);
    _analyserNode.getByteFrequencyData(buf);
    const avg = buf.reduce((a,b)=>a+b,0)/buf.length;
    document.getElementById('recMeterFill').style.width = Math.min(100,(avg/128)*100)+'%';
    bars.forEach((b,i) => { const v=buf[i%buf.length]||0; b.style.height=Math.max(4,(v/255)*36)+'px'; });
  }
  animWave();
}

function stopRecording() {
  if (_mediaRecorder && _mediaRecorder.state !== 'inactive') _mediaRecorder.stop();
  cleanupRecordingUI();
}

function cancelRecording() {
  _recordingCancelled = true;
  if (_mediaRecorder && _mediaRecorder.state !== 'inactive') _mediaRecorder.stop();
  _recordedChunks = [];
  cleanupRecordingUI();
  document.getElementById('recIdleState').style.display   = 'block';
  document.getElementById('recActiveState').style.display = 'none';
}

function cleanupRecordingUI() {
  clearInterval(_recTimerInterval);
  cancelAnimationFrame(_waveAnimId);
  document.getElementById('liveRecZone').classList.remove('recording');
  if (_micStream)     { _micStream.getTracks().forEach(t=>t.stop()); _micStream = null; }
  if (_audioContext)  { try { _audioContext.close(); } catch(e) {} _audioContext = null; }
}

function finalizeRecording() {
  if (!_recordedChunks.length) { toast('No audio recorded','error'); return; }
  const mimeType = _mediaRecorder.mimeType || 'audio/webm';
  const ext      = mimeType.includes('ogg')?'ogg':mimeType.includes('mp4')?'m4a':'webm';
  const blob     = new Blob(_recordedChunks, {type:mimeType});
  const filename = `engine_recording_${Date.now()}.${ext}`;
  state.audioFile = new File([blob], filename, {type:mimeType});
  document.getElementById('recActiveState').style.display  = 'none';
  document.getElementById('recIdleState').style.display    = 'block';
  document.getElementById('recordedFileName').textContent  = filename;
  document.getElementById('recordedFileMeta').textContent  = `${_recSeconds}s · ${(blob.size/1024).toFixed(1)} KB · ${mimeType}`;
  document.getElementById('recordedFileCard').style.display= 'block';
  const player = document.getElementById('recordedPlayerEl');
  player.src = URL.createObjectURL(blob); player.load();
  toast('Recording complete — ready to analyze','success');
}

function clearRecording() {
  state.audioFile = null; _recordedChunks = [];
  document.getElementById('recordedFileCard').style.display = 'none';
  document.getElementById('recActiveState').style.display   = 'none';
  document.getElementById('recIdleState').style.display     = 'block';
  const p = document.getElementById('recordedPlayerEl'); p.pause(); p.src='';
  document.getElementById('audioResult').style.display = 'none';
}

async function analyzeRecordedAudio() {
  if (!state.audioFile) return toast('No recording available','error');
  await runEngineAnalysis();
}

let _alcTimer=null, _alcWave=null;
let _audioModeBeforeAnalysis = 'upload';
function showAudioLoader(filename) {
  _audioModeBeforeAnalysis = document.getElementById('audioModeRecord').style.display === 'block' ? 'record' : 'upload';
  document.getElementById('audioUploadCard').style.display     = 'none';
  document.getElementById('audioAnalysisLoader').style.display = 'block';
  document.getElementById('audioResult').style.display         = 'none';
  document.getElementById('alc-file-name').textContent         = filename||'Processing...';
  document.getElementById('alcProgressFill').style.width       = '0%';
  document.getElementById('alcProgressPct').textContent        = '0%';
  for (let i=0; i<5; i++) { const s=document.getElementById(`alcStep${i}`); if(s) s.classList.remove('active','done'); }
  const canvas = document.getElementById('alcWaveCanvas'); const ctx = canvas.getContext('2d'); let t=0;
  function draw() {
    canvas.width=canvas.offsetWidth; canvas.height=canvas.offsetHeight;
    const W=canvas.width,H=canvas.height; ctx.clearRect(0,0,W,H);
    ctx.lineWidth=1.5; ctx.strokeStyle='rgba(200,216,228,0.6)'; ctx.shadowBlur=6; ctx.shadowColor='rgba(200,216,228,0.3)';
    ctx.beginPath();
    for (let x=0; x<W; x++) {
      const amp=(H/2)*0.6*(0.5+0.5*Math.sin(x*0.015+t*0.5));
      const y=H/2+amp*Math.sin(x*0.04+t)+(amp*0.3)*Math.sin(x*0.084+t*1.3);
      x===0 ? ctx.moveTo(x,y) : ctx.lineTo(x,y);
    }
    ctx.stroke(); t+=0.06; _alcWave=requestAnimationFrame(draw);
  }
  draw();
  const steps=[[15,0],[35,1],[58,2],[78,3],[92,4]]; let si=0;
  _alcTimer = setInterval(() => {
    if (si < steps.length) {
      const [pct,idx] = steps[si];
      if (si>0) { const prev=document.getElementById(`alcStep${steps[si-1][1]}`); if(prev){prev.classList.remove('active');prev.classList.add('done');} }
      const cur=document.getElementById(`alcStep${idx}`); if(cur) cur.classList.add('active');
      document.getElementById('alcProgressFill').style.width = pct+'%';
      document.getElementById('alcProgressPct').textContent  = pct+'%';
      si++;
    }
  }, 500);
}

function hideAudioLoader() {
  clearInterval(_alcTimer); cancelAnimationFrame(_alcWave);
  for (let i=0; i<5; i++) { const s=document.getElementById(`alcStep${i}`); if(s){s.classList.remove('active');s.classList.add('done');} }
  document.getElementById('alcProgressFill').style.width = '100%';
  document.getElementById('alcProgressPct').textContent  = '100%';
  setTimeout(() => {
    document.getElementById('audioAnalysisLoader').style.display = 'none';
    document.getElementById('audioUploadCard').style.display     = 'block';
    switchAudioMode(_audioModeBeforeAnalysis);
  }, 400);
}

let _audioRunning = false;
async function runEngineAnalysis() {
  if (_audioRunning || !state.audioFile) return;
  _audioRunning = true;
  const analyzeBtn = document.getElementById('btnAnalyzeAudio');
  const recordBtn  = document.getElementById('btnAnalyzeRecording');
  if (analyzeBtn) analyzeBtn.disabled = true;
  if (recordBtn)  recordBtn.disabled  = true;
  showAudioLoader(state.audioFile.name);
  const fd = new FormData(); fd.append('audio', state.audioFile);
  try {
    const res = await fetch(`${API}/analyze-engine`,{method:'POST',body:fd});
    if (!res.ok) throw new Error((await res.json()).detail || 'Audio analysis failed');
    const data = await res.json();
    state.audioResult = data;
    hideAudioLoader();
    renderAudioResult(data);
    document.getElementById('audioResult').style.display = 'block';
    document.getElementById('audioResult').scrollIntoView({behavior:'smooth',block:'nearest'});
    toast(data.is_knock ? '⚠ Engine knock detected!' : '✓ Engine sounds healthy', data.is_knock ? 'error' : 'success');
  } catch(e) {
    hideAudioLoader();
    toast(e.message,'error');
  }
  _audioRunning = false;
  if (analyzeBtn) analyzeBtn.disabled = false;
  if (recordBtn)  recordBtn.disabled  = false;
}

function renderAudioResult(data) {
  const el        = document.getElementById('audioResult');
  const cls       = data.is_knock ? 'knock' : 'clean';
  const icon      = data.is_knock ? '⚠️' : '✅';
  const headline  = data.is_knock ? 'KNOCK DETECTED' : 'ENGINE HEALTHY';
  const subtitle  = data.is_knock
    ? 'Pre-detonation signatures found — mechanic inspection recommended'
    : 'No knock signatures detected — engine sounds healthy';
  const topScores = [...(data.scores||[])].sort((a,b)=>b.score-a.score).slice(0,5);
  el.innerHTML = `<div class="audio-result-rich ${cls}">
    <div class="arr-header"><div class="arr-verdict-badge">${icon}</div><div class="arr-verdict-text"><h3>${headline}</h3><p>${subtitle}</p></div></div>
    <div class="arr-body">
      <div class="arr-metrics">
        <div class="arr-metric"><div class="arr-metric-val">${data.confidence||0}%</div><div class="arr-metric-lbl">Confidence</div></div>
        <div class="arr-metric"><div class="arr-metric-val">${data.duration_s||0}s</div><div class="arr-metric-lbl">Duration</div></div>
        <div class="arr-metric"><div class="arr-metric-val" style="font-size:.9rem">${(data.verdict||'N/A').slice(0,12)}</div><div class="arr-metric-lbl">Verdict Label</div></div>
      </div>
      ${topScores.length ? `<div class="arr-scores-title">Model Confidence Breakdown</div>${topScores.map(s=>`<div class="arr-score-row"><div class="arr-score-label">${s.label}</div><div class="arr-score-bar"><div class="arr-score-fill" style="width:0%" data-w="${(s.score*100).toFixed(1)}"></div></div><div class="arr-score-pct">${(s.score*100).toFixed(1)}%</div></div>`).join('')}` : ''}
      <div class="action-row" style="margin-top:16px">
        <button style="background:rgba(200,216,228,.08);color:var(--c);border:1px solid rgba(200,216,228,.25);padding:10px 20px;border-radius:8px;font-size:.875rem;font-weight:600;cursor:pointer;font-family:'Syne',sans-serif;display:inline-flex;align-items:center;gap:8px" onclick="resetAudioInput()">↺ Try Another</button>
        <button class="btn btn-primary" onclick="nextStep(4)" style="padding:10px 20px;font-size:.875rem">Continue to Report →</button>
      </div>
    </div>
  </div>`;
  setTimeout(() => { el.querySelectorAll('.arr-score-fill').forEach(b => { b.style.width=b.dataset.w+'%'; }); }, 80);
}

function buildSummaryCards() {
  const vin   = document.getElementById('vin').value   || 'N/A';
  const make  = document.getElementById('make').value  || 'N/A';
  const model = document.getElementById('model').value || 'N/A';
  const defects   = state.batchDefects || [];
  const defCount  = new Set(defects.map(d => (Array.isArray(d)?d[0]:d.label||'').trim().toLowerCase()).filter(Boolean)).size;
  const ar        = state.audioResult;
  const audioSt   = ar ? (ar.is_knock ? '⚠ Knock' : '✓ Healthy') : '—';
  document.getElementById('summaryCards').innerHTML = `
    <div class="panel" style="text-align:center;padding:20px">
      <div style="font-size:.75rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:8px">Vehicle</div>
      <div style="font-weight:700;font-size:1.05rem">${make} ${model}</div>
      <div style="font-size:.8rem;color:var(--td);margin-top:4px">${vin}</div>
    </div>
    <div class="panel" style="text-align:center;padding:20px">
      <div style="font-size:.75rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:8px">Defect Types</div>
      <div style="font-weight:700;font-size:2rem;color:${defCount>2?'var(--red)':defCount>0?'var(--yellow)':'var(--green)'}">${defCount}</div>
    </div>
    <div class="panel" style="text-align:center;padding:20px">
      <div style="font-size:.75rem;color:var(--tm);text-transform:uppercase;letter-spacing:.08em;font-family:'JetBrains Mono',monospace;margin-bottom:8px">Engine Audio</div>
      <div style="font-weight:700;font-size:1.2rem;color:${ar?(ar.is_knock?'var(--red)':'var(--green)'):'var(--td)'}"> ${audioSt}</div>
    </div>`;
}

function appendVehicleInfo(fd) {
  fd.append('vin',     document.getElementById('vin').value     || '');
  fd.append('make',    document.getElementById('make').value    || '');
  fd.append('model',   document.getElementById('model').value   || '');
  fd.append('year',    document.getElementById('year').value    || '');
  fd.append('mileage', document.getElementById('mileage').value || '');
  const ar = state.audioResult;
  if (ar) {
    fd.append('engine_verdict',    ar.verdict    || '');
    fd.append('engine_is_knock',   String(ar.is_knock));
    fd.append('engine_confidence', String(ar.confidence));
    fd.append('engine_duration',   String(ar.duration_s));
  }
}

// ── AI Analysis Panel renderer ─────────────────────────────────────────────
function renderAIPanel(ai) {
  const panel = document.getElementById('aiAnalysisPanel');
  if (!panel) return;
  const score   = ai.health_score    || 0;
  const risk    = (ai.risk_level     || 'Low').toLowerCase();
  const riskLbl = ai.risk_level      || 'Low';
  const summary = ai.summary         || '';
  const recs    = ai.recommendations || [];
  const factors = ai.risk_factors    || [];
  const scoreColor = score >= 75 ? '#4ade80' : score >= 50 ? '#f59e0b' : '#ff4444';
  const R    = 34;
  const CIRC = 2 * Math.PI * R;
  const dash = CIRC - (score / 100) * CIRC;
  const scoreDesc = score >= 85 ? 'Excellent condition'
    : score >= 70 ? 'Good — minor issues'
    : score >= 50 ? 'Fair — attention needed'
    : score >= 30 ? 'Poor — repairs required'
    : 'Critical — unsafe';
  const barStyle = score >= 75 ? 'background:#4ade80' : score >= 50 ? 'background:#f59e0b' : 'background:#ff4444';
  const riskEmoji = risk==='low'?'🟢': risk==='medium'?'🟡': risk==='high'?'🟠':'🔴';
  panel.innerHTML = `
    <div class="ai-panel">
      <div class="ai-panel-header">
        <span class="ai-panel-badge">✦ Groq AI</span>
        <span class="ai-panel-title">AI Vehicle Health Assessment</span>
      </div>
      <div class="ai-panel-body">
        <div class="health-score-wrap">
          <div class="health-score-ring">
            <svg viewBox="0 0 90 90">
              <circle class="hsr-track" cx="45" cy="45" r="${R}"/>
              <circle class="hsr-fill" cx="45" cy="45" r="${R}"
                stroke="${scoreColor}"
                stroke-dasharray="${CIRC}"
                stroke-dashoffset="${CIRC}"
                id="healthRingFill"/>
            </svg>
            <div class="health-score-val">
              <span class="hsv-num" style="color:${scoreColor}">${score}</span>
              <span class="hsv-lbl">/ 100</span>
            </div>
          </div>
          <div class="health-score-info">
            <div class="hsi-label">Vehicle Health Score</div>
            <div class="hsi-bar"><div class="hsi-bar-fill" id="healthBarFill" style="width:0%;${barStyle}"></div></div>
            <div class="hsi-desc">${scoreDesc}</div>
          </div>
        </div>
        <div class="risk-card">
          <div class="risk-card-label">Risk Assessment</div>
          <div class="risk-badge-lg ${risk}">${riskEmoji} ${riskLbl} Risk</div>
          ${factors.length ? `<div class="risk-factors-list">${factors.map(f=>`<div class="rf-item">${f}</div>`).join('')}</div>` : ''}
        </div>
        <div class="ai-recs-box">
          <div class="ai-recs-label">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
            AI Recommendations
          </div>
          ${recs.map((r,i)=>`<div class="ai-rec-item"><div class="ai-rec-num">${i+1}</div><div>${r}</div></div>`).join('')}
        </div>
        <div class="ai-summary-box">
          <div class="ai-summary-label">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#93c5fd" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
            AI Summary — Powered by Groq LLaMA 3.3
          </div>
          <div class="ai-summary-text">${summary}</div>
        </div>
      </div>
    </div>`;
  panel.style.display = 'block';
  requestAnimationFrame(() => requestAnimationFrame(() => {
    const ring = document.getElementById('healthRingFill');
    if (ring) ring.style.strokeDashoffset = String(dash);
    const bar  = document.getElementById('healthBarFill');
    if (bar)  bar.style.width = score + '%';
  }));
}

function showAILoading() {
  const panel = document.getElementById('aiAnalysisPanel');
  if (!panel) return;
  panel.innerHTML = `
    <div class="ai-panel">
      <div class="ai-panel-header">
        <span class="ai-panel-badge">✦ Groq AI</span>
        <span class="ai-panel-title">AI Vehicle Health Assessment</span>
      </div>
      <div class="ai-loading-state">
        <div class="spinner spinner-light"></div>
        <p>Groq LLaMA 3.3 is analysing your vehicle...</p>
      </div>
    </div>`;
  panel.style.display = 'block';
}

// ── Generate report ────────────────────────────────────────────────────────
let _generateRunning = false;
async function generateReport() {
  if (_generateRunning) return;
  if (!state.batchDone) return toast('Please complete body inspection first','error');
  _generateRunning = true;
  showAILoading();
  const btnGen  = document.getElementById('btnGenerate');
  const loading = document.getElementById('generateLoading');
  btnGen.style.display  = 'none';
  loading.style.display = 'flex';

  function restoreGenerateUI() {
    loading.style.display = 'none';
    btnGen.style.display  = 'inline-flex';
    _generateRunning      = false;
    document.getElementById('aiAnalysisPanel').style.display = 'none';
  }

  try {
    if (state.liveCameraResult) {
      const fd = new FormData(); appendVehicleInfo(fd);
      const res = await fetch(`${API}/finalize-live-detection`,{method:'POST',body:fd});
      if (!res.ok) throw new Error((await res.json()).detail || 'Report failed');
      const data = await res.json();
      state.reportReady = true;
      if (data.defects_detected && data.defects_detected.length) {
        state.batchDefects = data.defects_detected;
        state.liveCameraResult.defects_detected    = data.defects_detected;
        state.liveCameraResult.unique_defect_types = data.unique_defect_types || 0;
      }
      loading.style.display = 'none';
      _generateRunning      = false;
      toast('✓ Report generated!','success');
      if (data.ai_analysis) renderAIPanel(data.ai_analysis);
      renderResults({
        ...data,
        defects_detected: data.defects_detected || state.batchDefects,
        annotated_images: data.annotated_images || state.batchAnnotated,
      });
    } else {
      let filteredDefects   = [];
      let filteredAnnotated = [];
      if (state.perImageDefects.length > 0) {
        state.perImageDefects.forEach((img,i) => {
          if (!state.crossedIndices.has(i)) {
            filteredDefects.push(...img.defects);
            if (img.annotated) filteredAnnotated.push(img.annotated);
          }
        });
      } else {
        filteredDefects   = [...state.batchDefects];
        filteredAnnotated = state.batchAnnotated.filter((_,i) => !state.crossedIndices.has(i));
      }
      const ar      = state.audioResult;
      const payload = {
        defects_detected:    filteredDefects,
        annotated_images:    filteredAnnotated,
        image_count:         state.files.length - state.crossedIndices.size,
        unique_defect_types: new Set(filteredDefects.map(d=>(Array.isArray(d)?d[0]:d.label||'').toLowerCase()).filter(Boolean)).size,
        vehicle_info: {
          vin:     document.getElementById('vin').value     || '',
          make:    document.getElementById('make').value    || '',
          model:   document.getElementById('model').value   || '',
          year:    document.getElementById('year').value    || '',
          mileage: document.getElementById('mileage').value || '',
        },
        engine_result: ar ? {verdict:ar.verdict||'',is_knock:ar.is_knock,confidence:ar.confidence||0,duration_s:ar.duration_s||0} : null
      };
      const res = await fetch(`${API}/generate-report`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});
      if (!res.ok) throw new Error((await res.json()).detail || 'Report failed');
      const data = await res.json();
      state.reportReady = true;
      loading.style.display = 'none';
      _generateRunning      = false;
      toast('✓ Report generated!','success');
      if (data.ai_analysis) renderAIPanel(data.ai_analysis);
      renderResults({...data, defects_detected:filteredDefects, annotated_images:filteredAnnotated});
    }
  } catch(e) {
    toast(e.message,'error');
    restoreGenerateUI();
  }
}

function renderResults(data) {
  const defects     = data.defects_detected || [];
  const defectsNorm = defects.map(d => Array.isArray(d) ? d : [d.label||d.class||'Unknown', d.confidence||0]);
  const ut          = data.unique_defect_types != null
    ? data.unique_defect_types
    : new Set(defectsNorm.map(d=>(d[0]||'').toLowerCase()).filter(Boolean)).size;
  const verdict = ut===0?'PASS':ut<=2?'ATTENTION':'FAIL';
  const vc      = verdict==='PASS'?'pass':verdict==='ATTENTION'?'attention':'fail';
  const grouped = {};
  defectsNorm.forEach(([n,c]) => {
    const k = (n||'Unknown').toLowerCase();
    if (!grouped[k]) grouped[k] = {name:n||'Unknown',confs:[]};
    grouped[k].confs.push(c);
  });
  const gl       = Object.values(grouped).map(g=>({name:g.name,max:Math.max(...g.confs)})).sort((a,b)=>b.max-a.max);
  const annotated= data.annotated_images || [];
  const ar       = data.engine_result || state.audioResult;
  const ts       = Date.now();
  let html = `<div class="results-panel">
    <div class="result-hero">
      <div class="verdict-box ${vc}">
        <div class="verdict-label">Overall Verdict</div>
        <div class="verdict-status">${verdict}</div>
        <div class="verdict-sub">${ut} unique defect type${ut!==1?'s':''} detected</div>
      </div>
      <div class="panel" style="padding:20px">
        <div style="font-size:.8rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--tm);margin-bottom:14px;font-family:'JetBrains Mono',monospace">Detected Components</div>
        ${gl.length
          ? `<div class="defect-list">${gl.map(d=>`<div class="ditem"><div class="ditem-name">${d.name}</div><div class="ditem-bar"><div class="ditem-fill" style="width:${d.max}%"></div></div><div class="ditem-conf">${d.max}%</div></div>`).join('')}</div>`
          : `<div style="color:var(--green);font-weight:600;padding:8px 0">✓ No damage detected</div>`}
      </div>
    </div>`;
  if (ar) {
    const ec = ar.is_knock ? 'knock' : 'clean';
    html += `<div class="panel" style="border:1px solid ${ec==='knock'?'rgba(255,68,68,.3)':'rgba(74,222,128,.3)'};background:${ec==='knock'?'rgba(255,68,68,.04)':'rgba(74,222,128,.04)'};padding:20px">
      <div style="font-size:.8rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--tm);margin-bottom:12px;font-family:'JetBrains Mono',monospace">Engine Audio Result</div>
      <div style="display:flex;align-items:center;gap:12px"><div style="font-size:2rem">${ar.is_knock?'⚠️':'✅'}</div>
      <div><div style="font-weight:700;font-size:1rem;margin-bottom:4px">${ar.is_knock?'KNOCK DETECTED':'ENGINE HEALTHY'}</div>
      <div style="font-size:.82rem;color:var(--tm);font-family:'JetBrains Mono',monospace">Verdict: ${ar.verdict||'N/A'} — Confidence: ${ar.confidence||0}%</div></div></div></div>`;
  }
  if (annotated.length) {
    html += `<div class="panel"><div style="font-size:.8rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--tm);margin-bottom:14px;font-family:'JetBrains Mono',monospace">Annotated Images (${annotated.length})</div>
      <div class="ann-grid">${annotated.map((p,i)=>`<div class="ann-img"><img src="${API}/${p}?t=${ts}" alt="Annotated ${i+1}" onerror="this.alt='N/A'"><div class="ann-img-label">Image ${i+1}</div></div>`).join('')}</div></div>`;
  }
  html += `<div class="action-row" style="margin-top:8px">
    <a href="${API}/report?t=${ts}" target="_blank" class="btn btn-primary" style="text-decoration:none"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>Download PDF Report</a>
    <button class="btn btn-outline" onclick="goTo('history')">View in History</button>
    <button class="btn btn-ghost" onclick="startNewInspection()">Start New Inspection</button>
  </div></div>`;
  const area = document.getElementById('resultsArea');
  area.innerHTML = html; area.style.display = 'block';
  if (pdfOption === 'preview') {
    const pdfSection = document.getElementById('pdfViewerSection');
    document.getElementById('pdfViewerFrame').src = `${API}/report?t=${ts}`;
    pdfSection.style.display = 'block';
  }
  area.scrollIntoView({behavior:'smooth',block:'start'});
}

function startNewInspection() {
  _recordingCancelled = true;
  if (_mediaRecorder && _mediaRecorder.state !== 'inactive') {
    try { _mediaRecorder.stop(); } catch(e) {}
  }
  cleanupRecordingUI();
  lcStopStreamOnly();
  _lcStopInProgress = false;
  _batchRunning     = false;
  _audioRunning     = false;
  _generateRunning  = false;
  state = freshState();
  lcAllDefectsCollected = [];
  ['vin','make','model','year','mileage'].forEach(id => document.getElementById(id).value = '');
  document.getElementById('imgPreviewGrid').innerHTML   = '';
  document.getElementById('btnAnalyze').style.display   = 'none';
  document.getElementById('inlineResults').classList.remove('visible');
  document.getElementById('audioResult').style.display        = 'none';
  document.getElementById('audioUploadCard').style.display     = 'block';
  document.getElementById('audioAnalysisLoader').style.display = 'none';
  document.getElementById('audioDropZone').style.display       = 'block';
  document.getElementById('audioFileCard').style.display       = 'none';
  document.getElementById('recordedFileCard').style.display    = 'none';
  document.getElementById('recIdleState').style.display        = 'block';
  document.getElementById('recActiveState').style.display      = 'none';
  document.getElementById('audioInput').value                  = '';
  const ap = document.getElementById('audioPlayerEl');    ap.pause(); ap.src='';
  const rp = document.getElementById('recordedPlayerEl'); rp.pause(); rp.src='';
  document.getElementById('resultsArea').style.display     = 'none';
  document.getElementById('pdfViewerSection').style.display= 'none';
  document.getElementById('aiAnalysisPanel').style.display = 'none';
  document.getElementById('btnGenerate').style.display     = 'inline-flex';
  document.getElementById('generateLoading').style.display = 'none';
  document.getElementById('mscUpload').classList.add('active');
  document.getElementById('mscLive').classList.remove('active');
  switchAudioMode('upload');
  setPdfOption('preview');
  setStep(1);
  toast('Ready for new inspection','info');
}
</script>
</body>
</html>